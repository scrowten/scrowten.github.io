<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://scrowten.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://scrowten.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-02-28T02:51:06+00:00</updated><id>https://scrowten.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">🕌 I Built a Tool to Translate Arabic Islamic Texts — Fully Offline</title><link href="https://scrowten.github.io/blog/2026/introducing-tarjim/" rel="alternate" type="text/html" title="🕌 I Built a Tool to Translate Arabic Islamic Texts — Fully Offline"/><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>https://scrowten.github.io/blog/2026/introducing-tarjim</id><content type="html" xml:base="https://scrowten.github.io/blog/2026/introducing-tarjim/"><![CDATA[<h2 id="why-i-built-this">Why I Built This</h2> <p>I wanted to read kitab — classical Arabic Islamic scholarly texts — but I don’t read Arabic fluently enough to get through them on my own. These books carry centuries of knowledge: fiqh, aqeedah, tafsir, hadith commentary. Scholars have written in Arabic for over a millennium, and most of that wisdom has never been fully translated into Indonesian or English.</p> <p>My copy of <em>Al-Qawa’id Al-Arba’</em> by Imam Muhammad ibn Abd al-Wahhab sits as a scanned PDF. So does <em>Ifadatul Mustafid</em>. Most of the kitab I’ve collected are scanned images — not searchable text, just photographs of old book pages.</p> <p>The existing options were frustrating:</p> <ul> <li><strong>Google Translate</strong> works on typed text, not on scanned PDFs</li> <li><strong>Commercial tools</strong> either require internet, cost money, or don’t handle Arabic well</li> <li><strong>Copying text</strong> from a scanned PDF is impossible — it’s just pixels</li> </ul> <p>So I built <strong>Tarjim</strong> (تَرجِم — Arabic for “translate”).</p> <hr/> <h2 id="what-tarjim-does">What Tarjim Does</h2> <p>Tarjim takes a scanned Arabic PDF, runs OCR to extract the text line by line, translates each line offline, and writes the translated text back onto the same PDF — replacing the original Arabic in-place.</p> <p>The result is a new PDF that looks like the original layout, but in English (or Indonesian, or other languages).</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  Scanned Arabic PDF  (just images of Arabic text)
            ↓
Output: Translated PDF  (English/Indonesian text in the same positions)
</code></pre></div></div> <p>The entire pipeline runs <strong>offline</strong> after the initial model download. No API keys. No internet. No data leaves your machine. This was a hard requirement for me — these are religious texts, and privacy matters.</p> <hr/> <h2 id="how-it-works">How It Works</h2> <p>The pipeline has five steps:</p> <pre><code class="language-mermaid">graph TD
    A[Scanned Arabic PDF] --&gt; B[Render pages to images&lt;br/&gt;PyMuPDF @ 300 DPI]
    B --&gt; C[Arabic OCR&lt;br/&gt;Surya OCR → text lines + bounding boxes]
    C --&gt; D{Direct package available?}
    D --&gt;|ar → en| E[Direct translation&lt;br/&gt;Argos Translate]
    D --&gt;|ar → id / others| F[Pivot translation&lt;br/&gt;ar → en → id]
    E --&gt; G[Overlay translated text&lt;br/&gt;PIL with white box + word-wrap]
    F --&gt; G
    G --&gt; H[Save as new PDF]
</code></pre> <h3 id="step-1--render-pdf-to-images">Step 1 — Render PDF to Images</h3> <p>Since the input is a scanned document (images, not text), we first render each page into a high-resolution PIL image using PyMuPDF at 300 DPI. Higher DPI means better OCR accuracy.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">zoom</span> <span class="o">=</span> <span class="mi">300</span> <span class="o">/</span> <span class="mf">72.0</span>  <span class="c1"># PDF baseline is 72 DPI
</span><span class="n">matrix</span> <span class="o">=</span> <span class="n">pymupdf</span><span class="p">.</span><span class="nc">Matrix</span><span class="p">(</span><span class="n">zoom</span><span class="p">,</span> <span class="n">zoom</span><span class="p">)</span>
<span class="n">pix</span> <span class="o">=</span> <span class="n">page</span><span class="p">.</span><span class="nf">get_pixmap</span><span class="p">(</span><span class="n">matrix</span><span class="o">=</span><span class="n">matrix</span><span class="p">)</span>
<span class="n">image</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="nf">frombytes</span><span class="p">(</span><span class="sh">"</span><span class="s">RGB</span><span class="sh">"</span><span class="p">,</span> <span class="p">(</span><span class="n">pix</span><span class="p">.</span><span class="n">width</span><span class="p">,</span> <span class="n">pix</span><span class="p">.</span><span class="n">height</span><span class="p">),</span> <span class="n">pix</span><span class="p">.</span><span class="n">samples</span><span class="p">)</span>
</code></pre></div></div> <h3 id="step-2--ocr-with-surya">Step 2 — OCR with Surya</h3> <p>This is the most critical step. I tried three OCR engines before settling on Surya:</p> <ul> <li><strong>Tesseract</strong> — The classic. Arabic support exists, but accuracy on old scanned texts is poor. Struggles with naskh and ruq’ah scripts.</li> <li><strong>PaddleOCR</strong> — Better accuracy, but heavier setup and the Arabic model needed extra configuration.</li> <li><strong>Surya OCR</strong> — Built specifically for document-level OCR, supports Arabic out of the box, and returns text lines with precise bounding boxes. This is what I use.</li> </ul> <p>Surya returns a structured prediction: for each page, you get a list of <code class="language-plaintext highlighter-rouge">text_lines</code>, each with <code class="language-plaintext highlighter-rouge">.text</code> (the recognized Arabic string) and <code class="language-plaintext highlighter-rouge">.bbox</code> (the pixel coordinates <code class="language-plaintext highlighter-rouge">x1, y1, x2, y2</code>).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">surya.recognition</span> <span class="kn">import</span> <span class="n">RecognitionPredictor</span>
<span class="kn">from</span> <span class="n">surya.detection</span> <span class="kn">import</span> <span class="n">DetectionPredictor</span>

<span class="n">recognition_predictor</span> <span class="o">=</span> <span class="nc">RecognitionPredictor</span><span class="p">(</span><span class="n">foundation_predictor</span><span class="p">)</span>
<span class="n">detection_predictor</span> <span class="o">=</span> <span class="nc">DetectionPredictor</span><span class="p">()</span>

<span class="n">predictions</span> <span class="o">=</span> <span class="nf">recognition_predictor</span><span class="p">([</span><span class="n">image</span><span class="p">],</span> <span class="n">det_predictor</span><span class="o">=</span><span class="n">detection_predictor</span><span class="p">)</span>
<span class="n">page</span> <span class="o">=</span> <span class="n">predictions</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>

<span class="k">for</span> <span class="n">line</span> <span class="ow">in</span> <span class="n">page</span><span class="p">.</span><span class="n">text_lines</span><span class="p">:</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">text</span><span class="p">)</span>   <span class="c1"># "بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيمِ"
</span>    <span class="nf">print</span><span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">bbox</span><span class="p">)</span>   <span class="c1"># (42, 120, 890, 158)
</span></code></pre></div></div> <h3 id="step-3--translate-with-argos-translate">Step 3 — Translate with Argos Translate</h3> <p>Argos Translate is a fully offline, open-source machine translation library backed by OpenNMT models. Once packages are downloaded, no network is needed.</p> <p>The interesting engineering problem here was supporting Indonesian (Bahasa Indonesia) — there is no direct Arabic → Indonesian package in Argos. The solution: <strong>pivot translation</strong>.</p> <p>I’ll explain this in detail in the next section.</p> <h3 id="step-4--overlay-translated-text">Step 4 — Overlay Translated Text</h3> <p>For each detected text line, I:</p> <ol> <li>Draw a <strong>white rectangle</strong> over the original Arabic text (covering it completely)</li> <li>Calculate an appropriate font size based on the bounding box height</li> <li>Draw the translated text with <strong>word-wrapping</strong> to fit inside the same bounding box</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Cover the original Arabic
</span><span class="n">draw</span><span class="p">.</span><span class="nf">rectangle</span><span class="p">([</span><span class="n">x1</span><span class="p">,</span> <span class="n">y1</span><span class="p">,</span> <span class="n">x2</span><span class="p">,</span> <span class="n">y2</span><span class="p">],</span> <span class="n">fill</span><span class="o">=</span><span class="sh">"</span><span class="s">white</span><span class="sh">"</span><span class="p">,</span> <span class="n">outline</span><span class="o">=</span><span class="sh">"</span><span class="s">white</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Calculate font size from bbox height
</span><span class="n">font_size</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="nf">int</span><span class="p">((</span><span class="n">y2</span> <span class="o">-</span> <span class="n">y1</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.8</span><span class="p">))</span>
<span class="n">font</span> <span class="o">=</span> <span class="n">ImageFont</span><span class="p">.</span><span class="nf">truetype</span><span class="p">(</span><span class="n">font_path</span><span class="p">,</span> <span class="n">font_size</span><span class="p">)</span>

<span class="c1"># Word-wrap and draw translated text
</span><span class="nf">draw_text_in_box</span><span class="p">(</span><span class="n">draw</span><span class="p">,</span> <span class="n">translated_text</span><span class="p">,</span> <span class="n">bbox</span><span class="p">,</span> <span class="n">font</span><span class="p">)</span>
</code></pre></div></div> <p>There’s also a <code class="language-plaintext highlighter-rouge">clean</code> mode that starts from a blank white page — useful if you just want the translated text without the original scanned image behind it.</p> <h3 id="step-5--save-as-pdf">Step 5 — Save as PDF</h3> <p>All modified page images are saved back into a multi-page PDF using Pillow.</p> <hr/> <h2 id="the-engineering-decision-im-most-proud-of-smart-translation-routing">The Engineering Decision I’m Most Proud Of: Smart Translation Routing</h2> <p>This was the most interesting design challenge.</p> <p>Argos Translate has language packages that you install individually — <code class="language-plaintext highlighter-rouge">ar→en</code>, <code class="language-plaintext highlighter-rouge">en→id</code>, <code class="language-plaintext highlighter-rouge">en→fr</code>, etc. There is an <code class="language-plaintext highlighter-rouge">ar→en</code> package (Arabic to English). But there is <strong>no</strong> <code class="language-plaintext highlighter-rouge">ar→id</code> package (Arabic to Indonesian).</p> <p>So how do you translate Arabic to Indonesian? You chain two translations:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Arabic text → [ar→en model] → English text → [en→id model] → Indonesian text
</code></pre></div></div> <p>This is called <strong>pivot translation through English</strong>.</p> <p>The challenge is making this seamless. A user just wants to say <code class="language-plaintext highlighter-rouge">--lang id</code> and get Indonesian output. They shouldn’t need to know about pivoting.</p> <p>Here’s how I implemented it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">setup_argos_translation</span><span class="p">(</span><span class="n">from_code</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">to_code</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Returns </span><span class="sh">'</span><span class="s">direct</span><span class="sh">'</span><span class="s"> or </span><span class="sh">'</span><span class="s">pivot:en</span><span class="sh">'</span><span class="s"> depending on available packages.
    Auto-installs whatever is needed.
    </span><span class="sh">"""</span>
    <span class="c1"># 1. Try to find a direct package (e.g., ar→id)
</span>    <span class="n">direct_package</span> <span class="o">=</span> <span class="nf">find_package</span><span class="p">(</span><span class="n">from_code</span><span class="p">,</span> <span class="n">to_code</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">direct_package</span><span class="p">:</span>
        <span class="nf">install</span><span class="p">(</span><span class="n">direct_package</span><span class="p">)</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">direct</span><span class="sh">"</span>

    <span class="c1"># 2. No direct package — install pivot packages (ar→en + en→id)
</span>    <span class="nf">install</span><span class="p">(</span><span class="nf">find_package</span><span class="p">(</span><span class="n">from_code</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">))</span>
    <span class="nf">install</span><span class="p">(</span><span class="nf">find_package</span><span class="p">(</span><span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">,</span> <span class="n">to_code</span><span class="p">))</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">pivot:en</span><span class="sh">"</span>
</code></pre></div></div> <p>And in <code class="language-plaintext highlighter-rouge">translate_text()</code>, if the pair is known to be a pivot pair, it chains two calls:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">translate_text</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">from_code</span><span class="o">=</span><span class="sh">"</span><span class="s">ar</span><span class="sh">"</span><span class="p">,</span> <span class="n">to_code</span><span class="o">=</span><span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">):</span>
    <span class="k">if</span> <span class="sh">"</span><span class="s">ar-&gt;id</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">_PIVOT_PAIRS</span><span class="p">:</span>
        <span class="c1"># Two-step: ar → en → id
</span>        <span class="n">en_text</span> <span class="o">=</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="sh">"</span><span class="s">ar</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">en_text</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Direct: ar → en
</span>    <span class="k">return</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">from_code</span><span class="p">,</span> <span class="n">to_code</span><span class="p">)</span>
</code></pre></div></div> <p>The system remembers which pairs need pivoting at runtime. It even <strong>auto-discovers</strong> the need: if a direct translation call fails, it automatically retries via the English pivot and marks that pair for future calls.</p> <p>The route selection is fully transparent through the CLI:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ python -m src.cli -i kitab.pdf -o kitab_id.pdf --lang id --verbose

Translation route: ar → en → id (pivot through English, no direct package available)
</code></pre></div></div> <h3 id="translation-routing-table">Translation Routing Table</h3> <table> <thead> <tr> <th>Target Language</th> <th>Route</th> <th>Packages Needed</th> </tr> </thead> <tbody> <tr> <td>English (<code class="language-plaintext highlighter-rouge">en</code>)</td> <td>Direct</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code></td> </tr> <tr> <td>Indonesian (<code class="language-plaintext highlighter-rouge">id</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→id</code></td> </tr> <tr> <td>Malay (<code class="language-plaintext highlighter-rouge">ms</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→ms</code></td> </tr> <tr> <td>French (<code class="language-plaintext highlighter-rouge">fr</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→fr</code></td> </tr> <tr> <td>German (<code class="language-plaintext highlighter-rouge">de</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→de</code></td> </tr> <tr> <td>Spanish (<code class="language-plaintext highlighter-rouge">es</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→es</code></td> </tr> <tr> <td>Turkish (<code class="language-plaintext highlighter-rouge">tr</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→tr</code></td> </tr> </tbody> </table> <hr/> <h2 id="getting-started">Getting Started</h2> <h3 id="installation">Installation</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/scrowten/tarjim.git
<span class="nb">cd </span>tarjim

python <span class="nt">-m</span> venv venv
<span class="nb">source </span>venv/bin/activate  <span class="c"># Windows: venv\Scripts\activate</span>

pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p>On first run, Surya OCR models (~2–3 GB) and Argos translation packages download automatically. After that: fully offline.</p> <h3 id="translate-arabic--english">Translate Arabic → English</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> src.cli <span class="nt">--input</span> kitab.pdf <span class="nt">--output</span> kitab_en.pdf <span class="nt">--lang</span> en
</code></pre></div></div> <h3 id="translate-arabic--indonesian">Translate Arabic → Indonesian</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> src.cli <span class="nt">--input</span> kitab.pdf <span class="nt">--output</span> kitab_id.pdf <span class="nt">--lang</span> <span class="nb">id</span>
</code></pre></div></div> <p>The system automatically uses the ar→en→id pivot route. You don’t need to do anything special.</p> <h3 id="web-ui">Web UI</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn src.api:app <span class="nt">--port</span> 8000
<span class="c"># Open http://localhost:8000</span>
</code></pre></div></div> <p>Upload a PDF, pick a language, click Translate. The UI shows the translation route so you know what’s happening under the hood.</p> <h3 id="docker">Docker</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> tarjim <span class="nb">.</span>
docker run <span class="nt">-p</span> 8000:8000 tarjim
</code></pre></div></div> <p>The Docker image pre-downloads both the <code class="language-plaintext highlighter-rouge">ar→en</code> and <code class="language-plaintext highlighter-rouge">en→id</code> packages at build time.</p> <h3 id="python-api">Python API</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">src.core.pdf_handler</span> <span class="kn">import</span> <span class="n">process_pdf</span>

<span class="c1"># Arabic → Indonesian
</span><span class="nf">process_pdf</span><span class="p">(</span>
    <span class="n">input_path</span><span class="o">=</span><span class="sh">"</span><span class="s">al-qawaid-al-arba.pdf</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_path</span><span class="o">=</span><span class="sh">"</span><span class="s">al-qawaid-al-arba_id.pdf</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">target_lang</span><span class="o">=</span><span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">overlay_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># or "clean" for white background
</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="project-structure">Project Structure</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tarjim/
├── src/
│   ├── cli.py                    # Command-line interface
│   ├── api.py                    # FastAPI web server
│   └── core/
│       ├── pdf_handler.py        # Pipeline orchestrator
│       ├── ocr_surya.py          # Surya OCR wrapper
│       ├── translator_argos.py   # Argos Translate + pivot routing
│       └── utils.py              # Text overlay, fonts, word-wrap
├── static/
│   ├── index.html                # Web UI
│   └── styles.css
├── tests/                        # Unit tests
├── Dockerfile
└── requirements.txt
</code></pre></div></div> <p>The core modules are intentionally small and single-purpose. <code class="language-plaintext highlighter-rouge">translator_argos.py</code> knows nothing about PDFs. <code class="language-plaintext highlighter-rouge">ocr_surya.py</code> knows nothing about translation. <code class="language-plaintext highlighter-rouge">pdf_handler.py</code> orchestrates everything.</p> <hr/> <h2 id="challenges--lessons-learned">Challenges &amp; Lessons Learned</h2> <p><strong>OCR accuracy on old texts is hard.</strong> Surya is the best open-source option available, but classical Arabic manuscript fonts are different from modern typeset text. Vowel diacritics (<em>tashkeel</em>) especially trip up OCR models. For perfectly clean modern typeset kitab, results are good. For old handwritten manuscripts, there’s room to improve.</p> <p><strong>Translation quality through pivoting is lossy.</strong> Chaining <code class="language-plaintext highlighter-rouge">ar→en→id</code> means any error in the first step gets propagated and amplified in the second. The <code class="language-plaintext highlighter-rouge">ar→en</code> Argos model performs reasonably well on straightforward prose, but classical Arabic religious vocabulary — <em>tawhid</em>, <em>shirk</em>, <em>sunnah</em>, <em>bid’ah</em> — often gets transliterated rather than translated, which is actually appropriate for Islamic texts.</p> <p><strong>Font fitting is a solved problem — just tedious.</strong> Getting English text to fit into the same bounding box as Arabic text is fiddly. Arabic is compact horizontally; English words for the same concept are often longer. I handle this with dynamic font sizing and word-wrapping, but very dense Arabic pages still get tight.</p> <p><strong>Lazy imports matter for usability.</strong> Early versions of the code eagerly imported PyMuPDF and Surya at the top of every file. This meant that even <code class="language-plaintext highlighter-rouge">import src.core.utils</code> would try to load multi-gigabyte models. Moving to lazy imports (<code class="language-plaintext highlighter-rouge">__getattr__</code> on the package <code class="language-plaintext highlighter-rouge">__init__.py</code>) made the module usable in testing environments without all dependencies present.</p> <hr/> <h2 id="whats-next">What’s Next</h2> <p>The project is functional but there’s a clear path forward:</p> <ul> <li><strong>Better handling of dense pages</strong> — Auto-shrink font, or flow overflow text to margins</li> <li><strong>Bilingual output mode</strong> — Show original Arabic alongside the translation (side-by-side columns or interleaved pages)</li> <li><strong>Batch processing</strong> — Translate an entire folder of kitab PDFs in one command</li> <li><strong>Confidence scoring</strong> — Flag lines where OCR or translation confidence is low</li> <li><strong>Better support for tashkeel</strong> — Strip or handle vowel diacritics before OCR to improve accuracy</li> <li><strong>Table of contents preservation</strong> — Detect and translate structural elements, not just body text</li> <li><strong>Optional cloud API mode</strong> — Hook into a higher-quality translation API (OpenAI, Google) for users who prefer accuracy over privacy</li> </ul> <hr/> <h2 id="try-it">Try It</h2> <p>The project is open source under MIT license.</p> <p><strong>GitHub:</strong> <a href="https://github.com/scrowten/tarjim">github.com/scrowten/tarjim</a></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/scrowten/tarjim.git
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
python <span class="nt">-m</span> src.cli <span class="nt">--input</span> your_kitab.pdf <span class="nt">--output</span> translated.pdf <span class="nt">--lang</span> en
</code></pre></div></div> <p>If you work with Arabic documents — religious texts, academic papers, historical archives — I hope this tool is useful to you. Contributions, issues, and suggestions are very welcome.</p> <hr/> <p><em>Built with <a href="https://github.com/VikParuchuri/surya">Surya OCR</a>, <a href="https://github.com/argosopentech/argos-translate">Argos Translate</a>, <a href="https://github.com/pymupdf/PyMuPDF">PyMuPDF</a>, and <a href="https://fastapi.tiangolo.com/">FastAPI</a>.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="nlp"/><category term="open-source"/><category term="ocr"/><category term="translation"/><category term="arabic"/><category term="nlp"/><category term="python"/><summary type="html"><![CDATA[How I built Tarjim, an open-source offline OCR + translation pipeline for scanned Arabic kitab PDFs, using Surya OCR and Argos Translate with smart pivot routing for Indonesian.]]></summary></entry><entry><title type="html">🚀 Build Your First AI Agent with Google’s ADK (Part 1)</title><link href="https://scrowten.github.io/blog/2025/google-adk-part-1/" rel="alternate" type="text/html" title="🚀 Build Your First AI Agent with Google’s ADK (Part 1)"/><published>2025-11-24T14:25:00+00:00</published><updated>2025-11-24T14:25:00+00:00</updated><id>https://scrowten.github.io/blog/2025/google-adk-part-1</id><content type="html" xml:base="https://scrowten.github.io/blog/2025/google-adk-part-1/"><![CDATA[<h3 id="from-prompt--thought--action--result">From Prompt → Thought → Action → Result</h3> <p>AI is evolving fast—and it’s no longer limited to the classic <em>“you ask, it answers”</em> pattern.<br/> Today’s AI can do something far more powerful: <strong>act like an intelligent problem-solver</strong>.</p> <p>Modern AI <strong>agents</strong> don’t just generate text.</p> <p>They can <strong>think through a problem</strong>, <strong>decide what tools to use</strong>, <strong>take meaningful actions</strong>, and <strong>learn from the results</strong> before giving you a final answer.</p> <p>It’s like upgrading from a calculator… to a smart assistant that can research, reason, and execute tasks on your behalf.</p> <p>In this guide, you’ll get hands-on and build your <strong>first real AI agent</strong> using Google’s <strong>Agent Development Kit (ADK)</strong>—the same framework featured in the official Kaggle 5-Day Agents Course.</p> <p>No prior experience required. Just curiosity, a few lines of code, and you’re ready to create an AI that doesn’t just respond…<br/> <strong>it gets things done.</strong> 🚀</p> <p>By the end, you will:</p> <ul> <li>✔️ Install and configure ADK</li> <li>✔️ Set up Gemini API authentication</li> <li>✔️ Build your first agent with reasoning + tools</li> <li>✔️ Watch your agent perform an action (Google Search!)</li> <li>✔️ Explore the ADK Web Interface</li> </ul> <p>Let’s begin. 🚀</p> <hr/> <h1 id="️-1-setup">⚙️ 1. Setup</h1> <h2 id="11-install-dependencies">1.1 Install Dependencies</h2> <p>If you’re using Kaggle Notebooks, <strong>google-adk</strong> is already installed. To install it locally:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>google-adk
</code></pre></div></div> <hr/> <h2 id="12-configure-your-gemini-api-key">1.2 Configure Your Gemini API Key</h2> <p>ADK uses <strong>Gemini models</strong>, which require authentication.</p> <h3 id="1-create-an-api-key">1) Create an API Key</h3> <p>Go to <strong>Google AI Studio</strong> → Generate an API key.</p> <h3 id="2-add-your-key-to-your-environment">2) Add your key to your environment</h3> <p>Inside your project, create a <code class="language-plaintext highlighter-rouge">.env</code> file:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">GOOGLE_API_KEY</span><span class="o">=</span><span class="s2">"YOUR_KEY_HERE"</span>
</code></pre></div></div> <hr/> <h2 id="13-import-adk-components">1.3 Import ADK Components</h2> <p>These components give your agent LLM reasoning, tools, and execution runtime.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>
<span class="kn">from</span> <span class="n">google.adk.models.google_llm</span> <span class="kn">import</span> <span class="n">Gemini</span>
<span class="kn">from</span> <span class="n">google.adk.runners</span> <span class="kn">import</span> <span class="n">InMemoryRunner</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">google_search</span>
<span class="kn">from</span> <span class="n">google.genai</span> <span class="kn">import</span> <span class="n">types</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ ADK components imported successfully.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="14-configure-retry-behavior">1.4 Configure Retry Behavior</h2> <p>LLMs sometimes hit rate limits or temporary service issues. Retries help make your agent more reliable.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">retry_config</span> <span class="o">=</span> <span class="n">types</span><span class="p">.</span><span class="nc">HttpRetryOptions</span><span class="p">(</span>
    <span class="n">attempts</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>            <span class="c1"># Max retries
</span>    <span class="n">exp_base</span><span class="o">=</span><span class="mi">7</span><span class="p">,</span>            <span class="c1"># Exponential backoff multiplier
</span>    <span class="n">initial_delay</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>       <span class="c1"># Delay before first retry
</span>    <span class="n">http_status_codes</span><span class="o">=</span><span class="p">[</span><span class="mi">429</span><span class="p">,</span> <span class="mi">500</span><span class="p">,</span> <span class="mi">503</span><span class="p">,</span> <span class="mi">504</span><span class="p">]</span>  <span class="c1"># Retryable HTTP error codes
</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h1 id="-2-build-your-first-ai-agent">🤖 2. Build Your First AI Agent</h1> <h2 id="21-what-is-an-ai-agent">2.1 What <em>Is</em> an AI Agent?</h2> <p>A normal LLM:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prompt → LLM → Text
</code></pre></div></div> <p>An AI agent:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prompt → Agent → Thought → Action → Observation → Final Answer
</code></pre></div></div> <p>Your agent will be able to:</p> <ul> <li>Interpret a question</li> <li>Decide whether to search the web</li> <li>Use Google Search</li> <li>Combine results into a final answer</li> </ul> <hr/> <h2 id="22-define-your-agent">2.2 Define Your Agent</h2> <p>Here we describe:</p> <ul> <li><strong>Name</strong> &amp; <strong>description</strong></li> <li><strong>Model</strong> (Gemini 2.5 Flash Lite)</li> <li><strong>Instructions</strong> (how the agent should behave)</li> <li><strong>Tools</strong> (the actions it can take — we give it Google Search)</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">root_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">helpful_assistant</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span><span class="o">=</span><span class="nc">Gemini</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.5-flash-lite</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">retry_options</span><span class="o">=</span><span class="n">retry_config</span>
    <span class="p">),</span>
    <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">A simple agent that can answer general questions.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">You are a helpful assistant. Use Google Search for current info or if unsure.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span><span class="o">=</span><span class="p">[</span><span class="n">google_search</span><span class="p">],</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ Root Agent defined.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="23-run-your-agent">2.3 Run Your Agent</h2> <p>To run an agent, you need a <strong>Runner</strong>. This acts as the orchestrator that:</p> <ul> <li>Keeps track of the conversation</li> <li>Sends messages to the agent</li> <li>Handles tool calls &amp; results</li> </ul> <h3 id="a-create-the-runner">a) Create the Runner</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">runner</span> <span class="o">=</span> <span class="nc">InMemoryRunner</span><span class="p">(</span><span class="n">agent</span><span class="o">=</span><span class="n">root_agent</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ Runner created.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="b-ask-the-agent-a-question">b) Ask the Agent a Question</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">runner</span><span class="p">.</span><span class="nf">run_debug</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">What is Agent Development Kit from Google? What languages is the SDK available in?</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div> <p>This method:</p> <ul> <li>Creates a temporary session</li> <li>Allows the agent to think</li> <li>Lets it call Google Search</li> <li>Returns the final answer</li> </ul> <p>Perfect for prototyping!</p> <hr/> <h2 id="24-what-just-happened">2.4 What Just Happened?</h2> <p>Your agent:</p> <ol> <li>Read your question</li> <li>Realized it needed <strong>current information</strong></li> <li>Called the <strong>Google Search tool</strong></li> <li>Reviewed results</li> <li>Generated the final answer</li> </ol> <p>This is the foundation of AI agents: <strong>LLM → reasoning → tool choice → action → observation → improved answer</strong></p> <p>To see a full trace (thoughts, actions, observations), use the ADK Web UI next.</p> <hr/> <h1 id="-3-explore-the-adk-web-interface">💻 3. Explore the ADK Web Interface</h1> <p>ADK includes a browser-based UI for:</p> <ul> <li>Debugging agents</li> <li>Viewing traces</li> <li>Inspecting tool calls</li> <li>Exploring sessions</li> </ul> <h2 id="31-create-a-sample-agent">3.1 Create a Sample Agent</h2> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adk create sample-agent <span class="nt">--model</span> gemini-2.5-flash-lite <span class="nt">--api_key</span> <span class="nv">$GOOGLE_API_KEY</span>
</code></pre></div></div> <p>This generates:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sample-agent/
├── agent.py
├── .env
└── __init__.py
</code></pre></div></div> <h2 id="32-get-your-proxy-url-kaggle-only">3.2 Get Your Proxy URL (Kaggle only)</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">url_prefix</span> <span class="o">=</span> <span class="nf">get_adk_proxy_url</span><span class="p">()</span>
</code></pre></div></div> <h2 id="33-launch-the-web-ui">3.3 Launch the Web UI</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adk web <span class="nt">--url_prefix</span> <span class="o">{</span>url_prefix<span class="o">}</span>
</code></pre></div></div> <p>📌 <strong>Important:</strong> <strong>Do NOT share your proxy link</strong> — it contains an authentication token.</p> <hr/> <h1 id="-congratulations">🎉 Congratulations!</h1> <p>You just created your <strong>first AI agent</strong> using Google’s ADK!</p> <p>Key takeaways:</p> <ul> <li>Agents ≠ plain LLMs</li> <li>Agents <strong>reason</strong>, <strong>use tools</strong>, and <strong>act</strong></li> <li>ADK makes building agents simple and structured</li> <li>You now have a working agent that can search the internet</li> </ul> <p>Next steps:</p> <ul> <li>Try adding more tools</li> <li>Build multi-agent systems</li> <li>Explore advanced session management</li> <li>Deploy agents in real applications</li> </ul> <hr/>]]></content><author><name></name></author><category term="machine-learning"/><category term="llm"/><category term="agents"/><category term="adk"/><category term="agents"/><category term="llm"/><summary type="html"><![CDATA[Concise agentic tutorial using Google's ADK]]></summary></entry><entry><title type="html">Demystifying Visual Transformer (ViT)</title><link href="https://scrowten.github.io/blog/2021/vit-paper/" rel="alternate" type="text/html" title="Demystifying Visual Transformer (ViT)"/><published>2021-08-19T21:01:00+00:00</published><updated>2021-08-19T21:01:00+00:00</updated><id>https://scrowten.github.io/blog/2021/vit-paper</id><content type="html" xml:base="https://scrowten.github.io/blog/2021/vit-paper/"><![CDATA[<h1 id="-paper-summary-an-image-is-worth-16x16-words--transformers-for-image-recognition-at-scale">🧠 Paper Summary: <em>An Image is Worth 16x16 Words — Transformers for Image Recognition at Scale</em></h1> <p><strong>Authors:</strong> Alexey Dosovitskiy et al.<br/> <strong>Published by:</strong> Google Research, Brain Team (2020)<br/> <strong>Link:</strong> <a href="https://arxiv.org/pdf/2010.11929">arxiv.org/pdf/2010.11929</a></p> <hr/> <h2 id="-overview">📌 Overview</h2> <p>This paper introduces the <strong>Vision Transformer (ViT)</strong> — the first major attempt to apply a <strong>Transformer architecture</strong>, originally designed for NLP, directly to image classification tasks.<br/> The key idea: instead of processing pixels through convolutional layers, <strong>an image is split into patches</strong>, each treated as a “token” (similar to words in a sentence).</p> <p>ViT demonstrates that with <strong>sufficient data and compute</strong>, Transformers can outperform CNNs in image recognition — marking a major shift in computer vision research.</p> <hr/> <h2 id="-key-concepts">🧩 Key Concepts</h2> <h3 id="1-patch-embedding">1. <strong>Patch Embedding</strong></h3> <ul> <li>The input image (e.g., 224×224) is divided into fixed-size <strong>patches</strong> (e.g., 16×16 pixels).</li> <li>Each patch is <strong>flattened</strong> and linearly projected to form a <strong>vector embedding</strong>.</li> <li>These patch embeddings serve as input tokens for the Transformer encoder.</li> </ul> <h3 id="2-positional-encoding">2. <strong>Positional Encoding</strong></h3> <ul> <li>Since Transformers have no inherent notion of spatial order, <strong>positional embeddings</strong> are added to patch embeddings to preserve spatial relationships.</li> </ul> <h3 id="3-transformer-encoder">3. <strong>Transformer Encoder</strong></h3> <ul> <li>The encoder consists of <strong>multi-head self-attention (MHSA)</strong> and <strong>MLP blocks</strong>, similar to the standard NLP Transformer (Vaswani et al., 2017).</li> <li>Layer normalization and residual connections are applied after each sub-layer.</li> </ul> <h3 id="4-classification-token-cls">4. <strong>Classification Token ([CLS])</strong></h3> <ul> <li>A special learnable token is prepended to the sequence of patch embeddings.</li> <li>The final representation of this token is used for <strong>image classification</strong>.</li> </ul> <hr/> <h2 id="️-training-setup">⚙️ Training Setup</h2> <ul> <li><strong>Pretraining Dataset:</strong> JFT-300M (≈300M images, 18k classes) and ImageNet-21k (14M images, 21k classes).</li> <li><strong>Fine-tuning:</strong> After pretraining, the model is fine-tuned on smaller datasets like ImageNet, CIFAR-100, and VTAB.</li> <li><strong>Optimization:</strong> Adam optimizer with a cosine learning rate schedule and heavy data augmentation.</li> </ul> <hr/> <h2 id="-results">📈 Results</h2> <table> <thead> <tr> <th>Model</th> <th>Pretraining Dataset</th> <th>ImageNet Accuracy</th> <th>Params</th> </tr> </thead> <tbody> <tr> <td>ViT-B/16</td> <td>ImageNet-21k</td> <td>84.0%</td> <td>86M</td> </tr> <tr> <td>ViT-L/16</td> <td>JFT-300M</td> <td>88.5%</td> <td>307M</td> </tr> <tr> <td>ViT-H/14</td> <td>JFT-300M</td> <td>90.0%</td> <td>632M</td> </tr> </tbody> </table> <ul> <li>ViT outperforms <strong>ResNet</strong> and other CNN-based models when pretrained on large datasets.</li> <li>On smaller datasets (without large-scale pretraining), ViT underperforms due to weaker inductive biases compared to CNNs.</li> </ul> <hr/> <h2 id="-insights--contributions">🧠 Insights &amp; Contributions</h2> <ol> <li><strong>Transformers can scale to vision tasks</strong> with sufficient data and computational power.</li> <li><strong>CNN inductive biases</strong> (translation invariance, locality) are not strictly necessary for high performance — they can be learned from data.</li> <li>ViT models <strong>transfer well</strong> across different datasets via fine-tuning.</li> <li>Demonstrated the <strong>emerging scalability law</strong> — model performance increases predictably with data and compute.</li> </ol> <hr/> <h2 id="️-pros--cons">⚖️ Pros &amp; Cons</h2> <table> <thead> <tr> <th>Pros</th> <th>Cons</th> </tr> </thead> <tbody> <tr> <td>Simple, elegant architecture — no convolutions.</td> <td>Requires massive data for effective training.</td> </tr> <tr> <td>Strong transfer learning performance.</td> <td>Computationally expensive at large scale.</td> </tr> <tr> <td>Flexible — easily extended to detection, segmentation, etc.</td> <td>Poor inductive bias for small data settings.</td> </tr> </tbody> </table> <hr/> <h2 id="-follow-up-work">🔍 Follow-up Work</h2> <ul> <li><strong>DeiT (Data-efficient Image Transformer)</strong>: Improves ViT training efficiency with smaller datasets using knowledge distillation.</li> <li><strong>Swin Transformer</strong>: Introduces hierarchical structure and local attention windows for dense prediction tasks.</li> <li><strong>Hybrid Models</strong>: Combine CNN feature extractors with ViT-style attention blocks.</li> </ul> <hr/> <h2 id="-takeaway">🧭 Takeaway</h2> <blockquote> <p><strong>“ViT marks the turning point where pure Transformers became competitive with CNNs in computer vision — given enough data.”</strong></p> </blockquote> <p>It simplifies the architecture but relies on <strong>data scale</strong> and <strong>transfer learning</strong>, setting the foundation for the transformer revolution in visual tasks.</p> <hr/> <p><em>Prepared for technical sharing session — concise summary by Rizky.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformer"/><category term="machine-learning"/><category term="image-processing"/><category term="vit"/><summary type="html"><![CDATA[Paper Summary of Visual Transformer (ViT)]]></summary></entry></feed>