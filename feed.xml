<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://scrowten.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://scrowten.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-08T09:41:47+00:00</updated><id>https://scrowten.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">🏠 Why Run Your Own LLM? The Case for Self-Hosting in 2026</title><link href="https://scrowten.github.io/blog/2026/local-llm-why-self-host/" rel="alternate" type="text/html" title="🏠 Why Run Your Own LLM? The Case for Self-Hosting in 2026"/><published>2026-04-08T00:00:00+00:00</published><updated>2026-04-08T00:00:00+00:00</updated><id>https://scrowten.github.io/blog/2026/local-llm-why-self-host</id><content type="html" xml:base="https://scrowten.github.io/blog/2026/local-llm-why-self-host/"><![CDATA[<p><em>This is Part 1 of the <strong>Self-Hosting LLMs</strong> series — a practical guide to running open-source AI on your own hardware.</em></p> <hr/> <p>Two moments usually push developers toward running their own LLMs.</p> <p><strong>The first is the bill.</strong></p> <p>You build a small weekend project — a coding assistant, a document summarizer, a chatbot for your team. You wire it up to the OpenAI API, test it out, invite a few people. Two weeks later you check your usage dashboard and see a number you weren’t expecting.</p> <p><strong>The second is the pause.</strong></p> <p>You’re about to paste something into ChatGPT — a contract, some internal code, a customer email — and a small voice asks: <em>“wait, where does this actually go?”</em></p> <p>These two moments — <strong>cost</strong> and <strong>privacy</strong> — are why people start looking at local LLMs. And in 2026, the answer to both is better than most people expect.</p> <p>This post makes the case for self-hosting, shows you what hardware you realistically need, and gets you running a real model in about 5 minutes.</p> <hr/> <h1 id="️-1-what-self-hosting-actually-means">🗺️ 1. What “Self-Hosting” Actually Means</h1> <p>The term covers a few different setups — all of them mean <em>you</em> control the model, not a vendor:</p> <ul> <li><strong>Local</strong> — model runs on your laptop or desktop, right now</li> <li><strong>On-prem server</strong> — a machine in your office, home lab, or rack</li> <li><strong>Cloud self-hosted</strong> — you rent a GPU VM (Vast.ai, RunPod, AWS) and run your own model on it</li> </ul> <p>All three share the same property: no third party sits between your data and the model. The rest of this series focuses primarily on the local case — a gaming PC, a workstation, or an Apple Silicon Mac — because that’s where most developers start.</p> <hr/> <h1 id="-2-the-cost-problem">💸 2. The Cost Problem</h1> <p>Cloud LLM APIs have gotten significantly cheaper over the past two years. But at any meaningful scale, they’re still expensive.</p> <h3 id="current-api-pricing-april-2026">Current API Pricing (April 2026)</h3> <table> <thead> <tr> <th>Model</th> <th>Input (per 1M tokens)</th> <th>Output (per 1M tokens)</th> </tr> </thead> <tbody> <tr> <td>Claude Sonnet 4.6</td> <td>$3.00</td> <td>$15.00</td> </tr> <tr> <td>GPT-4o</td> <td>$2.50</td> <td>$10.00</td> </tr> <tr> <td>Gemini 2.5 Pro</td> <td>$2.00</td> <td>$12.00</td> </tr> <tr> <td>DeepSeek V3</td> <td>$0.14</td> <td>$0.28</td> </tr> </tbody> </table> <p>Output tokens are typically 3–10x more expensive than input tokens, and that’s where most of your spend goes — every word the model writes is output.</p> <h3 id="what-that-looks-like-in-practice">What That Looks Like in Practice</h3> <p>A moderate developer use case — say, a coding assistant running 10 million tokens per month — looks like this:</p> <ul> <li><strong>Claude Sonnet 4.6</strong>: ~$300–600/month</li> <li><strong>GPT-4o</strong>: ~$250–500/month</li> <li><strong>Gemini 2.5 Pro</strong>: ~$200–400/month</li> </ul> <p>For a side project, that’s real money. For a small team? It adds up fast. For high-volume production workloads — internal tooling, document pipelines, automated review systems — API costs often become the dominant infrastructure expense.</p> <h3 id="the-self-hosting-math">The Self-Hosting Math</h3> <p>An RTX 4090 costs about $1,600. Running Llama 3.1 70B on it, your cost per million tokens is roughly <strong>$0.01–0.02</strong> (electricity + hardware depreciation). That’s 100–1,000x cheaper than a premium cloud API.</p> <p>At 100 million tokens per month: cloud APIs cost $2,000–6,000. Self-hosted costs about $1–2.</p> <p><strong>Breakeven point:</strong></p> <ul> <li>Under 2M tokens/day → cloud APIs are likely cheaper (no idle infrastructure cost)</li> <li>2–5M tokens/day → roughly equivalent once you factor in maintenance time</li> <li>Over 5M tokens/day → self-hosting is dramatically cheaper</li> </ul> <p>If you’re building anything beyond occasional queries, the math starts tilting toward local surprisingly quickly.</p> <blockquote> <p><strong>Honest caveat</strong>: Don’t forget your time. Getting a local LLM running is easy (seriously, 5 minutes). Keeping it running in production, monitoring it, updating models — that takes real engineering effort. For small teams without DevOps capacity, APIs stay cheaper even at medium scale.</p> </blockquote> <hr/> <h1 id="-3-the-privacy-problem">🔒 3. The Privacy Problem</h1> <p>This one is subtler, and more important for some use cases than others.</p> <h3 id="what-happens-to-your-prompts">What Happens to Your Prompts?</h3> <p><strong>Consumer products</strong> (ChatGPT Plus, Claude Pro, Gemini Advanced): Training is typically enabled by default. You need to actively opt out, and opt-out only applies going forward. Your past conversations may already have been used.</p> <p><strong>APIs</strong> are better, but not zero-risk:</p> <ul> <li>OpenAI API: not used for training by default; data retained 30 days for abuse monitoring</li> <li>Anthropic API: not used for training; retained 7 days</li> <li>Both offer “Zero Data Retention” agreements for enterprise customers</li> </ul> <p>The practical question is: <strong>are you comfortable with your prompts sitting on US servers</strong>, even briefly, even if they’re not used for training?</p> <p>For many use cases, the answer is “sure, fine.” For others it’s not:</p> <ul> <li><strong>Healthcare</strong>: Sending patient information to a third-party US-hosted API can violate HIPAA data handling requirements</li> <li><strong>Legal</strong>: Client communications, contract details, case strategy — subject to attorney-client privilege and confidentiality</li> <li><strong>Finance</strong>: Trading research, internal financial projections, M&amp;A details — heavily regulated</li> <li><strong>European businesses</strong>: GDPR requires that personal data either stays in the EU or is sent somewhere with adequate protections. Sending it to a US API via standard terms is a grey area, and EU AI Act enforcement (August 2026) makes this harder to ignore</li> </ul> <h3 id="the-only-true-guarantee">The Only True Guarantee</h3> <p>If the model runs on your machine, your data never leaves your machine. No terms of service, no data retention policy, no regional compliance issue. It’s a simple guarantee that no cloud provider can match.</p> <p>For most developers building personal tools, this doesn’t matter much. For anyone handling sensitive data — or building tools for clients who do — it often matters a lot.</p> <hr/> <h1 id="-4-control-reliability-and-freedom">⚡ 4. Control, Reliability, and Freedom</h1> <p>Cost and privacy get the attention, but there’s a third category: <strong>control</strong>.</p> <h3 id="no-rate-limits">No Rate Limits</h3> <p>Cloud APIs throttle you. When you’re running a batch job at 2am, or your product suddenly gets popular, or you’re testing something intensively — rate limits kick in and slow you down. With a local model, the only limit is your hardware.</p> <h3 id="no-outages">No Outages</h3> <p>OpenAI, Anthropic, and Google all have status pages. They all have incidents. If your product depends on their API, their outage is your outage.</p> <h3 id="no-silent-model-changes">No Silent Model Changes</h3> <p>Cloud providers update their models regularly. Sometimes <code class="language-plaintext highlighter-rouge">gpt-4o</code> today behaves differently than <code class="language-plaintext highlighter-rouge">gpt-4o</code> three months ago — same name, different model, different behavior. This breaks evals, changes output formats, surprises users.</p> <p>With a local model, the file on your disk doesn’t change unless you change it. You can pin to an exact version forever.</p> <h3 id="works-offline">Works Offline</h3> <p>Plane, train, remote location, network outage, air-gapped environment — a local model doesn’t care.</p> <hr/> <h1 id="-5-but-is-it-actually-good-enough">🧠 5. But Is It Actually Good Enough?</h1> <p>Fair question. A year ago the honest answer was “for some things.” Today it’s closer to “for most things.”</p> <h3 id="coding--the-gap-has-closed">Coding — The Gap Has Closed</h3> <table> <thead> <tr> <th>Benchmark</th> <th>Cloud leader</th> <th>Best open-source</th> <th>Gap</th> </tr> </thead> <tbody> <tr> <td>HumanEval (coding)</td> <td>GPT-4o: 80.5%</td> <td>DeepSeek-Coder-V2: <strong>82.6%</strong></td> <td>None — open source leads</td> </tr> <tr> <td>SWE-bench (real GitHub issues)</td> <td>Claude: 80.9%</td> <td>DeepSeek: 78%</td> <td>Small</td> </tr> <tr> <td>General reasoning (MMLU)</td> <td>GPT-4o: 88.7%</td> <td>Llama 3.1 70B: 86.0%</td> <td>Small</td> </tr> <tr> <td>Complex math</td> <td>GPT-4o: leading</td> <td>Llama 3.1 70B: behind</td> <td>Noticeable</td> </tr> </tbody> </table> <p>For everyday coding tasks — completing functions, writing tests, explaining code, generating documentation — local models running at Q4 quantization are genuinely competitive with cloud APIs. DeepSeek-Coder-V2 running locally on a single GPU benchmarks above GPT-4o on HumanEval.</p> <h3 id="where-cloud-still-leads">Where Cloud Still Leads</h3> <ul> <li><strong>Complex multi-step reasoning</strong> — frontier models still have an edge on hard math and science problems</li> <li><strong>Very long context</strong> — 128K+ context with consistent quality is still cloud’s strength</li> <li><strong>Multimodal tasks</strong> — understanding images and documents is more mature in cloud models</li> </ul> <p>If your use case is: write code, summarize documents, answer questions, draft emails, build internal tools — a local 70B model will surprise you. If your use case is: solve graduate-level math problems or analyze dozens of images per request — cloud still has the edge.</p> <hr/> <h1 id="-6-what-hardware-do-you-actually-need">💻 6. What Hardware Do You Actually Need?</h1> <p>Less than you think.</p> <p>The key resource is <strong>VRAM</strong> (video RAM on your GPU). That’s what limits which models you can run and how fast they go. Part 3 of this series goes deep on <a href="/blog/2026/local-llm-quantization-explained/">quantization and VRAM math</a> — but here’s the practical summary:</p> <table> <thead> <tr> <th>What you have</th> <th>What you can run</th> <th>Quality</th> </tr> </thead> <tbody> <tr> <td>Any Mac M1/M2/M3/M4, 16GB</td> <td>13B models, comfortably</td> <td>Great</td> </tr> <tr> <td>Any Mac M1/M2/M3/M4, 8GB</td> <td>7B models</td> <td>Good</td> </tr> <tr> <td>Gaming PC, RTX 3060 12GB</td> <td>13B at Q4, 7B easily</td> <td>Great</td> </tr> <tr> <td>Gaming PC, RTX 4070 Ti 16GB</td> <td>30B at Q4</td> <td>Great</td> </tr> <tr> <td>Gaming PC, RTX 4090 24GB</td> <td>70B at Q4</td> <td>Excellent</td> </tr> <tr> <td>CPU only, no GPU</td> <td>7B models, slowly</td> <td>Usable</td> </tr> </tbody> </table> <p><strong>Apple Silicon is a special case.</strong> Unlike PCs where the GPU has its own dedicated VRAM pool, Apple’s M-series chips use unified memory — the same memory pool is shared between CPU, GPU, and Neural Engine. This means a 16GB MacBook Pro has 16GB available for model loading, with no separate VRAM limit. It’s one of the best consumer options for local LLMs today.</p> <p><strong>The honest minimum:</strong> A 7B model at Q4 quantization runs on almost anything built in the last 5 years. It won’t run fast on a laptop CPU, but it runs. For a meaningful experience, aim for 8GB+ of VRAM or an Apple Silicon Mac with 16GB.</p> <hr/> <h1 id="-7-the-4-tools-youll-encounter">🧰 7. The 4 Tools You’ll Encounter</h1> <p>The ecosystem has converged around four main options. We’ll go deep on each in Part 2, but here’s the lay of the land:</p> <p><strong>Ollama</strong> — One command downloads and runs any model. OpenAI-compatible API built in. The right starting point for almost everyone.</p> <p><strong>LM Studio</strong> — Ollama-level simplicity with a polished desktop UI. Great for people who prefer a GUI over a terminal.</p> <p><strong>llama.cpp</strong> — The C++ engine that powers most local inference under the hood. Maximum portability, CPU-friendly, runs on anything. More setup, but the most flexible.</p> <p><strong>vLLM</strong> — Built for production. Designed to serve many concurrent users with maximum throughput. Overkill for personal use; exactly right for team deployments.</p> <hr/> <h1 id="-8-your-first-5-minutes">🚀 8. Your First 5 Minutes</h1> <p>Theory aside — let’s run something.</p> <h3 id="install-ollama">Install Ollama</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># macOS / Linux</span>
curl <span class="nt">-fsSL</span> https://ollama.com/install.sh | sh

<span class="c"># Windows: download the installer from https://ollama.com</span>
</code></pre></div></div> <h3 id="run-your-first-model">Run Your First Model</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Llama 3.2 3B — small, fast, runs on almost any hardware (~2 GB)</span>
ollama run llama3.2:3b
</code></pre></div></div> <p>Ollama downloads the model automatically on first run. Then you get an interactive prompt:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt;&gt;&gt; What's the difference between a list and a tuple in Python?

Great question! The key differences are:

1. **Mutability** — lists are mutable (you can change them after creation),
   tuples are immutable (fixed once created).

2. **Syntax** — lists use square brackets [1, 2, 3],
   tuples use parentheses (1, 2, 3).

3. **Performance** — tuples are slightly faster to iterate over
   and use less memory.

4. **Use case** — use lists when data will change;
   use tuples for fixed data like coordinates or config values.
</code></pre></div></div> <p>That’s a real local LLM running on your hardware. No API key, no billing, no data leaving your machine.</p> <h3 id="or-use-the-api">Or Use the API</h3> <p>If you want to integrate with existing code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">(</span>
    <span class="n">base_url</span><span class="o">=</span><span class="sh">"</span><span class="s">http://localhost:11434/v1</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">ollama</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># required field, value ignored locally
</span><span class="p">)</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">llama3.2:3b</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Write a Python function to flatten a nested list.</span><span class="sh">"</span><span class="p">}],</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span><span class="p">)</span>
</code></pre></div></div> <p>This is the standard OpenAI Python client, pointed at a local server instead of <code class="language-plaintext highlighter-rouge">api.openai.com</code>. Any code that works with the OpenAI API works here with a one-line URL change.</p> <hr/> <h1 id="-is-self-hosting-right-for-you">🎯 Is Self-Hosting Right for You?</h1> <p><strong>Yes, if:</strong></p> <ul> <li>You’re building something that touches sensitive data</li> <li>You’re running high token volumes and the bill is growing</li> <li>You want a stable, offline-capable development environment</li> <li>You have a gaming PC or Apple Silicon Mac sitting idle</li> </ul> <p><strong>Not yet, if:</strong></p> <ul> <li>You need cutting-edge reasoning or vision capabilities</li> <li>You’re a small team with no DevOps capacity and low token volumes</li> <li>You need 100K+ token context windows reliably</li> </ul> <p>The good news: you don’t have to choose permanently. Many developers use local models for development and testing, then route production traffic to a cloud API for the tasks where quality matters most. The OpenAI-compatible API standard makes switching between local and cloud trivially easy — same code, different URL.</p> <hr/> <h1 id="-whats-next">📚 What’s Next</h1> <p>This series walks through everything you need to go from curious to confident:</p> <ul> <li><strong>Part 1</strong>: Why Self-Host? ← <em>you are here</em></li> <li><strong>Part 2</strong>: Framework Showdown — Ollama, LM Studio, llama.cpp, vLLM <em>(coming soon)</em></li> <li><strong>Part 3</strong>: <a href="/blog/2026/local-llm-quantization-explained/">Quantization Explained — How a 70B Model Fits on Your Laptop</a></li> <li><strong>Part 4</strong>: Open Source Model Landscape — Benchmarks and Picking the Right One <em>(coming soon)</em></li> </ul> <p>If you want to understand <em>why</em> a 70B model fits on a 24GB GPU, or what “Q4_K_M” means on a Hugging Face download page — jump to Part 3 now. Otherwise, Part 2 will walk you through the four main tools in detail.</p> <hr/> <p><em>Questions or corrections? Leave a comment below, or find me on GitHub at <a href="https://github.com/scrowten">@scrowten</a>.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="llm"/><category term="self-hosting"/><category term="llm"/><category term="local-ai"/><category term="self-hosting"/><category term="ollama"/><category term="privacy"/><summary type="html"><![CDATA[The case for running open-source LLMs yourself — cost math, privacy, capability parity, and what hardware you actually need. Part 1 of the Self-Hosting LLMs series.]]></summary></entry><entry><title type="html">🧮 Quantization Explained: How a 70B Model Fits on Your Laptop</title><link href="https://scrowten.github.io/blog/2026/local-llm-quantization-explained/" rel="alternate" type="text/html" title="🧮 Quantization Explained: How a 70B Model Fits on Your Laptop"/><published>2026-04-06T00:00:00+00:00</published><updated>2026-04-06T00:00:00+00:00</updated><id>https://scrowten.github.io/blog/2026/local-llm-quantization-explained</id><content type="html" xml:base="https://scrowten.github.io/blog/2026/local-llm-quantization-explained/"><![CDATA[<p><em>This is Part 3 of the <strong>Self-Hosting LLMs</strong> series. You can read this post standalone — no prior posts required.</em></p> <hr/> <p>You’ve seen it on Reddit or Hugging Face: <strong>“Llama 3.1 70B”</strong>.</p> <p>Seventy billion. That sounds enormous. Surely you need a server room or a $30,000 GPU cluster to run something like that, right?</p> <p>Probably not.</p> <p>With the right settings, a <strong>70B model can run on a single consumer GPU</strong> — or even mostly on your CPU if you’re patient. And a <strong>7B model runs beautifully on a laptop GPU</strong> with 8GB of VRAM.</p> <p>The technique that makes this possible is called <strong>quantization</strong>. It’s the single most important concept to understand when you’re running LLMs locally, and it’s not as scary as it sounds.</p> <p>By the end of this post you will:</p> <ul> <li>✔️ Understand what a “parameter” actually is and why it takes memory</li> <li>✔️ Know the difference between FP32, FP16, INT8, INT4 — and why it matters</li> <li>✔️ Be able to calculate how much VRAM a model will need, yourself</li> <li>✔️ Know when to use GGUF vs GPTQ vs AWQ</li> <li>✔️ Understand what a KV cache is and why it silently eats your VRAM</li> <li>✔️ Download and run a quantized model with a single command</li> </ul> <p>Let’s start from the beginning.</p> <hr/> <h1 id="-1-what-is-a-parameter">🧠 1. What Is a Parameter?</h1> <p>Before we talk about reducing model size, we need to understand what we’re actually reducing.</p> <p>A large language model is a neural network. At its core, it’s a massive mathematical function — one that takes text as input and predicts the next word. This function has billions of “tunable knobs” called <strong>parameters</strong> (also called <strong>weights</strong>).</p> <p>Think of it like a giant mixing board with 70 billion sliders. During training, the model learns to set each slider to the exact value that makes it good at language. Once training is done, those values are frozen — and when you run the model, the computer reads those slider values to produce its output.</p> <p>Every single parameter is a <strong>number</strong> that has to be stored in memory. And the precision of that number — how many decimal places it has — determines how much space it takes.</p> <p>That’s where precision formats come in.</p> <hr/> <h1 id="️-2-precision-formats-fp32-fp16-bf16-int8-int4">🎛️ 2. Precision Formats: FP32, FP16, BF16, INT8, INT4</h1> <p>Every number stored in a computer takes a fixed number of <strong>bits</strong> (the 0s and 1s that computers use). More bits = more precision = more memory used.</p> <p>Here’s how the common formats break down:</p> <table> <thead> <tr> <th>Format</th> <th>Bits per number</th> <th>Bytes per number</th> <th>Example: 7B model size</th> </tr> </thead> <tbody> <tr> <td>FP32 (full precision)</td> <td>32 bits</td> <td>4 bytes</td> <td>~28 GB</td> </tr> <tr> <td>FP16 (half precision)</td> <td>16 bits</td> <td>2 bytes</td> <td>~14 GB</td> </tr> <tr> <td>BF16 (brain float 16)</td> <td>16 bits</td> <td>2 bytes</td> <td>~14 GB</td> </tr> <tr> <td>INT8 (8-bit integer)</td> <td>8 bits</td> <td>1 byte</td> <td>~7 GB</td> </tr> <tr> <td>INT4 (4-bit integer)</td> <td>4 bits</td> <td>0.5 bytes</td> <td>~3.5 GB</td> </tr> </tbody> </table> <h3 id="fp32--the-original-format">FP32 — The “Original” Format</h3> <p>32-bit floating point. This is the standard precision used in math and training. Each number can represent values with high accuracy across a huge range.</p> <p>For a 70B parameter model: <strong>70,000,000,000 × 4 bytes = 280 GB</strong>.</p> <p>That’s more than three A100 GPUs (80 GB each) running simultaneously. Not practical for most of us.</p> <h3 id="fp16--bf16--half-precision">FP16 / BF16 — Half Precision</h3> <p>16-bit formats cut memory in half by being slightly less precise.</p> <ul> <li><strong>FP16</strong>: 5 exponent bits, 10 mantissa bits. More precise in the middle range.</li> <li><strong>BF16</strong>: 8 exponent bits, 7 mantissa bits. Same range as FP32, better for very large or very small values. Preferred for training, great for inference.</li> </ul> <p>In practice, FP16 and BF16 produce nearly identical output quality for inference. A 70B model in FP16: <strong>~140 GB</strong>. Still requires two H100s.</p> <h3 id="int8--8-bit-integer">INT8 — 8-bit Integer</h3> <p>Instead of a floating point number, each weight becomes an integer between -128 and 127. The model is calibrated so these integers map closely to the original values.</p> <p>Quality impact: Research shows <strong>only a 0.04% drop</strong> on standard benchmarks compared to FP16. Essentially free lunch.</p> <p>A 7B model in INT8: <strong>~7 GB</strong> — fits on a single RTX 3080 or Mac with 8GB unified memory.</p> <h3 id="int4--4-bit-integer">INT4 — 4-bit Integer</h3> <p>Four bits means only <strong>16 possible values</strong> (-8 to 7). This is where you’d expect quality to fall off a cliff — but it doesn’t, at least not dramatically.</p> <p>Studies on Llama 3.1 8B showed that good INT4 quantization retains <strong>98%+ accuracy</strong> on MMLU-Pro (a standard reasoning benchmark). The quality loss is real but surprisingly small for most everyday tasks.</p> <p>A 7B model in INT4: <strong>~3.5 GB</strong> — runs on anything with a halfway modern GPU, or even on CPU with patience.</p> <hr/> <h1 id="-3-the-math-calculate-vram-yourself">🔢 3. The Math: Calculate VRAM Yourself</h1> <p>Here’s the formula you can use for any model:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Model VRAM (GB) ≈ (Parameters in billions × Bits per weight) / 8 / 1024
</code></pre></div></div> <p>Let’s work through some examples:</p> <p><strong>Llama 3.1 8B in FP16:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8B params × 16 bits = 128 billion bits
128 billion bits ÷ 8 = 16 GB of bytes
≈ 16 GB VRAM
</code></pre></div></div> <p><strong>Llama 3.1 8B in Q4 (INT4):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8B params × 4 bits = 32 billion bits
32 billion bits ÷ 8 = 4 GB
≈ 4 GB VRAM
</code></pre></div></div> <p><strong>Llama 3.1 70B in Q4 (INT4):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>70B params × 4 bits = 280 billion bits
280 billion bits ÷ 8 = 35 GB
≈ 35 GB VRAM
</code></pre></div></div> <blockquote> <p>⚠️ <strong>Important</strong>: These numbers are for the <strong>model weights only</strong>. You’ll need additional VRAM for the KV cache (more on this in Section 5) and runtime overhead (~20% buffer is a safe estimate). Always add ~20% on top of the weight-only estimate.</p> </blockquote> <hr/> <h1 id="-4-quantization-formats-gguf-gptq-and-awq">📦 4. Quantization Formats: GGUF, GPTQ, and AWQ</h1> <p>Now that you understand <em>why</em> we quantize, let’s talk about <em>how</em>. There are several methods and file formats you’ll encounter in the wild.</p> <h3 id="gguf--the-one-for-most-people">GGUF — The One for Most People</h3> <p><strong>GGUF</strong> (GGML Universal Format) is a file format that packages the quantized model and its metadata in a single <code class="language-plaintext highlighter-rouge">.gguf</code> file. It’s the format used by <strong>Ollama</strong>, <strong>llama.cpp</strong>, and <strong>LM Studio</strong>.</p> <p>What makes GGUF special:</p> <ul> <li><strong>CPU-friendly</strong>: Unlike most GPU formats, GGUF works well on pure CPU</li> <li><strong>Mixed CPU+GPU</strong>: You can load some layers to GPU and the rest to RAM — useful when the model almost fits on your GPU</li> <li><strong>Portable</strong>: One file, no setup, no dependencies</li> <li><strong>Many quantization levels</strong>: Q2, Q3, Q4, Q5, Q6, Q8 — pick your precision</li> </ul> <p>GGUF is where most beginners should start.</p> <h3 id="gptq--for-pure-gpu-throughput">GPTQ — For Pure GPU Throughput</h3> <p><strong>GPTQ</strong> uses calibration data (a sample of text) to find optimal 4-bit integer values for each weight. The calibration process minimizes the error introduced by quantization.</p> <p>Key characteristics:</p> <ul> <li><strong>Requires GPU</strong>: Not designed for CPU inference</li> <li><strong>Faster on GPU</strong>: With optimized kernels (like Marlin), GPTQ can be ~5x faster than GGUF on the same GPU</li> <li><strong>Slightly better quality</strong> than naive INT4 quantization at same bit count</li> <li>Used heavily with <strong>vLLM</strong> and <strong>text-generation-inference</strong></li> </ul> <p>Choose GPTQ if you have a dedicated GPU and care about maximum throughput (tokens per second).</p> <h3 id="awq--the-smarter-4-bit">AWQ — The Smarter 4-bit</h3> <p><strong>AWQ</strong> (Activation-Aware Weight Quantization) takes a clever approach: not all weights are equally important. AWQ identifies which weights have the most influence on outputs (by analyzing activation patterns) and protects those with higher precision.</p> <p>Key characteristics:</p> <ul> <li><strong>No backpropagation</strong> needed during quantization (faster to produce than GPTQ)</li> <li><strong>Retains ~95% quality</strong> vs FP16</li> <li>Good balance of speed and quality</li> <li>Works well with <strong>vLLM</strong> and other GPU inference servers</li> </ul> <h3 id="quick-decision-guide">Quick Decision Guide</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Just getting started / using Ollama / using LM Studio?
  → GGUF (Q4_K_M is the default recommendation)

Have a dedicated NVIDIA GPU, want maximum speed?
  → GPTQ or AWQ (use with vLLM)

Running on CPU or splitting across CPU + GPU?
  → GGUF only
</code></pre></div></div> <hr/> <h1 id="-5-decoding-the-naming-convention">🔤 5. Decoding the Naming Convention</h1> <p>When you browse Hugging Face or Ollama’s model library, you’ll see names like:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>llama3.1:8b-instruct-q4_K_M
mistral:7b-instruct-v0.2-q5_K_S
deepseek-coder:6.7b-instruct-q8_0
</code></pre></div></div> <p>Let’s decode <code class="language-plaintext highlighter-rouge">q4_K_M</code>:</p> <table> <thead> <tr> <th>Part</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">q4</code></td> <td>4-bit quantization</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">K</code></td> <td>K-quants method (importance-weighted, smarter quantization)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">M</code></td> <td>Medium variant — balanced between size and quality</td> </tr> </tbody> </table> <p>The variants go: <code class="language-plaintext highlighter-rouge">S</code> (small) &lt; <code class="language-plaintext highlighter-rouge">M</code> (medium) &lt; <code class="language-plaintext highlighter-rouge">L</code> (large) — meaning <code class="language-plaintext highlighter-rouge">_S</code> is smaller/faster and <code class="language-plaintext highlighter-rouge">_L</code> is higher quality.</p> <p>There’s also the simpler <code class="language-plaintext highlighter-rouge">_0</code> suffix (like <code class="language-plaintext highlighter-rouge">q4_0</code> or <code class="language-plaintext highlighter-rouge">q8_0</code>) which uses the older, uniform quantization method. K-quants (with <code class="language-plaintext highlighter-rouge">_K_</code>) are generally better — they apply higher precision to the most important weights.</p> <h3 id="the-communitys-favorite-q4_k_m">The Community’s Favorite: Q4_K_M</h3> <p><strong>Q4_K_M is the sweet spot</strong> that the LocalLLaMA community has converged on for good reason:</p> <ul> <li>~75% size reduction vs FP16</li> <li>Retains ~92% of original quality</li> <li>Runs fast on consumer hardware</li> <li>The K-quant method gives ~10-30% better perplexity vs naive Q4</li> </ul> <p>When in doubt, start with Q4_K_M. If quality feels lacking, step up to Q5_K_M. If you’re tight on VRAM, try Q3_K_M — though quality starts to noticeably dip below Q4.</p> <h3 id="at-a-glance-quality-vs-size">At-a-Glance Quality vs Size</h3> <table> <thead> <tr> <th>Quantization</th> <th>Size (7B)</th> <th>Quality (vs FP16)</th> <th>Use When</th> </tr> </thead> <tbody> <tr> <td>Q8_0</td> <td>~7 GB</td> <td>~99%</td> <td>VRAM allows, maximum quality</td> </tr> <tr> <td>Q6_K</td> <td>~5.5 GB</td> <td>~98%</td> <td>Near-lossless, tighter VRAM</td> </tr> <tr> <td>Q5_K_M</td> <td>~4.8 GB</td> <td>~96%</td> <td>Great quality, slight size saving</td> </tr> <tr> <td><strong>Q4_K_M</strong></td> <td><strong>~4.1 GB</strong></td> <td><strong>~92%</strong></td> <td><strong>Default recommendation</strong></td> </tr> <tr> <td>Q3_K_M</td> <td>~3.3 GB</td> <td>~85%</td> <td>When you really need the space</td> </tr> <tr> <td>Q2_K</td> <td>~2.7 GB</td> <td>~75%</td> <td>Last resort, noticeable quality drop</td> </tr> </tbody> </table> <hr/> <h1 id="-6-the-kv-cache-the-hidden-vram-thief">💾 6. The KV Cache: The Hidden VRAM Thief</h1> <p>Here’s something most beginners don’t know until they run out of memory: the model weights are only <strong>part</strong> of the VRAM you need. There’s a silent consumer called the <strong>KV cache</strong>.</p> <h3 id="what-is-a-kv-cache">What Is a KV Cache?</h3> <p>When an LLM generates text, it does so one token at a time. For each token it generates, the model performs an “attention” calculation that looks back at all previous tokens to understand context.</p> <p>To avoid recomputing those attention values every single step, the model <strong>caches them</strong> in memory — this is the Key-Value (KV) cache.</p> <p>The KV cache grows with:</p> <ul> <li><strong>Context length</strong> — longer conversations = bigger cache</li> <li><strong>Batch size</strong> — more simultaneous users = bigger cache</li> </ul> <h3 id="how-much-does-it-cost">How Much Does It Cost?</h3> <p>For a <strong>Llama 3.1 8B</strong> model with a 4,096 token context window:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>KV cache ≈ 2 × context_length × num_layers × num_kv_heads × head_dim × 2 bytes (FP16)
         ≈ 2 × 4096 × 32 × 8 × 128 × 2 bytes
         ≈ ~0.5 GB
</code></pre></div></div> <p>That’s manageable for short conversations. But at <strong>128K context</strong> (which Llama 3.1 supports):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>KV cache ≈ ~17 GB just for one long conversation
</code></pre></div></div> <p>Suddenly the “4 GB model” needs 21 GB total. This surprises a lot of people.</p> <h3 id="kv-cache-quantization">KV Cache Quantization</h3> <p>You can also quantize the KV cache itself:</p> <table> <thead> <tr> <th>KV Cache Format</th> <th>VRAM Reduction</th> <th>Speed Impact</th> </tr> </thead> <tbody> <tr> <td>FP16 (standard)</td> <td>Baseline</td> <td>Fastest</td> </tr> <tr> <td>FP8</td> <td>~50% reduction</td> <td>Minimal impact</td> </tr> <tr> <td>INT8 (q8_0)</td> <td>~50% reduction</td> <td>&lt;5% slower — <strong>best tradeoff</strong></td> </tr> <tr> <td>INT4</td> <td>~75% reduction</td> <td>90%+ slower at long contexts — avoid</td> </tr> </tbody> </table> <p>The community recommendation: <strong>FP8 or q8_0 KV cache</strong> if your tool supports it, otherwise leave it at FP16. INT4 KV cache is not worth the speed penalty.</p> <h3 id="practical-takeaway">Practical Takeaway</h3> <p>When calculating “will this fit?”, always account for context:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Total VRAM needed ≈ Model weights + KV cache + ~20% overhead

Example: Llama 3.1 8B Q4_K_M for a normal conversation (2K context)
  = 4.1 GB (weights) + 0.25 GB (KV cache) + ~1 GB (overhead)
  = ~5.4 GB total
  → Fits on a 6 GB GPU with room to spare ✓
</code></pre></div></div> <hr/> <h1 id="-7-ram-offloading-last-resort-not-a-strategy">🐢 7. RAM Offloading: Last Resort, Not a Strategy</h1> <p>What happens when a model is too large to fit in your GPU’s VRAM? Some tools (especially Ollama and llama.cpp) offer <strong>RAM offloading</strong> — storing part of the model in your CPU’s regular RAM and loading weights on demand.</p> <p>It sounds like a lifesaver. In practice, it’s a significant performance hit.</p> <h3 id="how-much-slower">How Much Slower?</h3> <p>Running a model with weights partially in RAM (via system memory) is roughly <strong>20-30x slower</strong> than running entirely in VRAM. A model that produces 40 tokens/second from VRAM might produce 1-2 tokens/second with heavy offloading.</p> <p>With Ollama, you can see how layers are distributed:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ollama ps
</code></pre></div></div> <p>Output will show something like:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                    ID         SIZE      PROCESSOR    UNTIL
llama3.1:8b-q4_K_M     a6990ed6   5.2 GB    100% GPU     4 minutes from now
</code></pre></div></div> <p>vs</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                    ID         SIZE      PROCESSOR    UNTIL
llama3.1:70b-q4_K_M    ...        43.0 GB   38% GPU      4 minutes from now
                                              62% CPU
</code></pre></div></div> <p>That 62% CPU means 62% of the model’s layers are offloaded to RAM — and your throughput will feel it.</p> <h3 id="when-is-offloading-ok">When Is Offloading OK?</h3> <ul> <li><strong>Development/experimentation</strong>: Acceptable to test a model you wouldn’t deploy</li> <li><strong>Very short generations</strong>: If you just need to generate 50 tokens to test something, it’s tolerable</li> <li><strong>Night-time batch jobs</strong>: If speed doesn’t matter and you’re fine waiting</li> </ul> <p>For anything interactive (a chatbot, a coding assistant), offloading makes the experience frustrating. The solution is to use a smaller, more aggressively quantized model that fits fully in VRAM.</p> <hr/> <h1 id="-8-what-can-you-actually-run-hardware-cheat-sheet">💻 8. What Can You Actually Run? (Hardware Cheat Sheet)</h1> <p>Here’s a practical guide organized by how much VRAM (or unified memory) you have:</p> <table> <thead> <tr> <th>VRAM</th> <th>Hardware Examples</th> <th>What Fits (Comfortably)</th> </tr> </thead> <tbody> <tr> <td>4 GB</td> <td>GTX 1650, older integrated</td> <td>3B models at Q4, 7B at Q2 (barely)</td> </tr> <tr> <td>6 GB</td> <td>RTX 3060, GTX 1660</td> <td>7B at Q4_K_M ✓, 13B at Q2</td> </tr> <tr> <td>8 GB</td> <td>RTX 3070, M1 MacBook (8GB)</td> <td>7B at Q5_K_M ✓, 13B at Q3-Q4</td> </tr> <tr> <td>12 GB</td> <td>RTX 3080, M2 Pro (12GB)</td> <td>13B at Q4-Q5 ✓, 30B at Q2-Q3</td> </tr> <tr> <td>16 GB</td> <td>RTX 4070 Ti, M2 Max (16GB)</td> <td>13B at Q6-Q8 ✓, 30B at Q4 ✓</td> </tr> <tr> <td>24 GB</td> <td>RTX 4090, RTX 3090, A5000</td> <td>30B at FP16 ✓, 70B at Q3 ✓</td> </tr> <tr> <td>32 GB</td> <td>M3 Max (36GB), RTX 5090</td> <td>70B at Q4 ✓</td> </tr> <tr> <td>48 GB</td> <td>A6000, dual RTX 3090</td> <td>70B at Q5-Q6 ✓, small MoE models</td> </tr> <tr> <td>80 GB</td> <td>A100, H100</td> <td>70B at FP16 ✓, 405B at Q4</td> </tr> </tbody> </table> <blockquote> <p><strong>Apple Silicon note</strong>: Mac unified memory is shared between CPU and GPU, so it’s more efficient than discrete GPU+RAM setups. A MacBook Pro with 16GB unified memory can comfortably run 13B models and is one of the best consumer options for local LLMs today.</p> </blockquote> <h3 id="general-rule-of-thumb">General Rule of Thumb</h3> <p><strong>Find your VRAM, divide by model size in billions, look up the highest quality quant that fits:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>VRAM (GB) ÷ 0.5 ≈ max model size in billions at Q4
</code></pre></div></div> <p>So 8 GB VRAM → up to ~16B params at Q4. 24 GB → up to ~48B params at Q4.</p> <hr/> <h1 id="-9-hands-on-download-and-run-a-quantized-model">🚀 9. Hands-On: Download and Run a Quantized Model</h1> <p>Let’s make this concrete. We’ll use <strong>Ollama</strong> — the simplest way to get started.</p> <h3 id="install-ollama">Install Ollama</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># macOS / Linux</span>
curl <span class="nt">-fsSL</span> https://ollama.com/install.sh | sh

<span class="c"># Windows: download the installer from https://ollama.com</span>
</code></pre></div></div> <h3 id="pull-a-model-with-a-specific-quantization">Pull a Model With a Specific Quantization</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 7B model at Q4_K_M — the community sweet spot (~4 GB VRAM)</span>
ollama pull llama3.1:8b-instruct-q4_K_M

<span class="c"># 7B model at Q5_K_M — one step higher quality (~5 GB VRAM)</span>
ollama pull llama3.1:8b-instruct-q5_K_M

<span class="c"># If you have 24GB+ VRAM and want maximum quality</span>
ollama pull llama3.1:8b-instruct-q8_0
</code></pre></div></div> <p>If you just type the model name without a quantization tag, Ollama picks a sensible default (usually Q4_K_M):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ollama pull llama3.1:8b
</code></pre></div></div> <h3 id="check-whats-loaded">Check What’s Loaded</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># See which models are currently running and their VRAM usage</span>
ollama ps

<span class="c"># List all models you've downloaded</span>
ollama list
</code></pre></div></div> <h3 id="chat-with-it">Chat With It</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ollama run llama3.1:8b-instruct-q4_K_M
</code></pre></div></div> <p>Or use it via the API (which is OpenAI-compatible):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">(</span>
    <span class="n">base_url</span><span class="o">=</span><span class="sh">"</span><span class="s">http://localhost:11434/v1</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">api_key</span><span class="o">=</span><span class="sh">"</span><span class="s">ollama</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># required by the client, but ignored locally
</span><span class="p">)</span>

<span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
    <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">llama3.1:8b-instruct-q4_K_M</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">messages</span><span class="o">=</span><span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Explain quantization to a 10-year-old.</span><span class="sh">"</span><span class="p">}</span>
    <span class="p">],</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span><span class="p">)</span>
</code></pre></div></div> <p>This is the same OpenAI client you’d use with <code class="language-plaintext highlighter-rouge">api.openai.com</code> — just with a different <code class="language-plaintext highlighter-rouge">base_url</code>. That’s the beauty of the OpenAI-compatible API standard that all local tools implement.</p> <h3 id="experiment-feel-the-quality-difference">Experiment: Feel the Quality Difference</h3> <p>Try the same prompt on different quants and see if you notice a difference:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Lower quality</span>
ollama pull llama3.1:8b-instruct-q2_K
ollama run llama3.1:8b-instruct-q2_K <span class="s2">"Solve: if a train travels at 60 mph for 2.5 hours, how far does it go?"</span>

<span class="c"># Standard quality</span>
ollama run llama3.1:8b-instruct-q4_K_M <span class="s2">"Solve: if a train travels at 60 mph for 2.5 hours, how far does it go?"</span>
</code></pre></div></div> <p>For simple questions, both will answer correctly. For complex reasoning, Q2 starts to struggle noticeably. This is a great way to build intuition for where the quality floor actually is.</p> <hr/> <h1 id="-quick-reference-card">🎯 Quick Reference Card</h1> <p>Print this out or keep it handy:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CHOOSING A QUANTIZATION
─────────────────────────────────────────────
Have lots of VRAM, want best quality?  → Q8_0
Standard use, best balance?            → Q4_K_M  ← Start here
Tight on VRAM?                         → Q3_K_M
Really tight?                          → Q2_K (quality noticeably degrades)

CHOOSING A FORMAT
─────────────────────────────────────────────
Ollama / LM Studio / llama.cpp?        → GGUF
vLLM / GPU server?                     → GPTQ or AWQ
CPU or mixed CPU+GPU?                  → GGUF only

VRAM ESTIMATE
─────────────────────────────────────────────
Rough formula: params_B × bits / 8 / 1024 = GB
Add 20% overhead + KV cache (~0.5 GB per 4K context)

7B Q4:  ~4 GB weights → ~5-6 GB total
13B Q4: ~7 GB weights → ~9 GB total
70B Q4: ~35 GB weights → ~40+ GB total
</code></pre></div></div> <hr/> <h1 id="-wrapping-up">🎉 Wrapping Up</h1> <p>Quantization is what makes local LLMs actually practical. Instead of needing a $30,000 GPU cluster for a 70B model, you can run one on a gaming PC — with only a small, often barely-noticeable quality tradeoff.</p> <p>The key ideas to remember:</p> <ul> <li><strong>Parameters × bit width ÷ 8 = model size in bytes</strong> — the math is simple</li> <li><strong>Q4_K_M is the default recommendation</strong> — best size/quality balance for most people</li> <li><strong>GGUF for local/CPU/Ollama, GPTQ/AWQ for GPU servers</strong></li> <li><strong>Don’t forget the KV cache</strong> — it’s the hidden VRAM cost that surprises people</li> <li><strong>RAM offloading is a last resort</strong>, not a feature — go smaller/more quantized instead</li> </ul> <hr/> <h2 id="whats-next-in-the-series">What’s Next in the Series</h2> <p>This series covers everything you need to go from “I’ve heard of LLMs” to running them confidently in your own environment:</p> <ul> <li><strong>Part 1</strong>: Why Self-Host? The Local LLM Primer <em>(coming soon)</em></li> <li><strong>Part 2</strong>: Framework Showdown — Ollama, LM Studio, llama.cpp, vLLM <em>(coming soon)</em></li> <li><strong>Part 3</strong>: Quantization Explained ← <em>you are here</em></li> <li><strong>Part 4</strong>: Open Source Model Landscape — Benchmarks and Picking the Right Model <em>(coming soon)</em></li> </ul> <hr/> <p><em>Have questions or corrections? Leave a comment below, or find me on GitHub at <a href="https://github.com/scrowten">@scrowten</a>.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="llm"/><category term="self-hosting"/><category term="llm"/><category term="quantization"/><category term="local-ai"/><category term="ollama"/><category term="gguf"/><summary type="html"><![CDATA[A beginner-friendly but technically deep guide to LLM quantization — covering precision formats, GGUF/GPTQ/AWQ, VRAM math, KV cache, and RAM offloading. Part 3 of the Self-Hosting LLMs series.]]></summary></entry><entry><title type="html">🕌 I Built a Tool to Translate Arabic Islamic Texts — Fully Offline</title><link href="https://scrowten.github.io/blog/2026/introducing-tarjim/" rel="alternate" type="text/html" title="🕌 I Built a Tool to Translate Arabic Islamic Texts — Fully Offline"/><published>2026-02-28T00:00:00+00:00</published><updated>2026-02-28T00:00:00+00:00</updated><id>https://scrowten.github.io/blog/2026/introducing-tarjim</id><content type="html" xml:base="https://scrowten.github.io/blog/2026/introducing-tarjim/"><![CDATA[<h2 id="why-i-built-this">Why I Built This</h2> <p>I wanted to read kitab — classical Arabic Islamic scholarly texts — but I don’t read Arabic fluently enough to get through them on my own. These books carry centuries of knowledge: fiqh, aqeedah, tafsir, hadith commentary. Scholars have written in Arabic for over a millennium, and most of that wisdom has never been fully translated into Indonesian or English.</p> <p>My copy of <em>Al-Qawa’id Al-Arba’</em> by Imam Muhammad ibn Abd al-Wahhab sits as a scanned PDF. So does <em>Ifadatul Mustafid</em>. Most of the kitab I’ve collected are scanned images — not searchable text, just photographs of old book pages.</p> <p>The existing options were frustrating:</p> <ul> <li><strong>Google Translate</strong> works on typed text, not on scanned PDFs</li> <li><strong>Commercial tools</strong> either require internet, cost money, or don’t handle Arabic well</li> <li><strong>Copying text</strong> from a scanned PDF is impossible — it’s just pixels</li> </ul> <p>So I built <strong>Tarjim</strong> (تَرجِم — Arabic for “translate”).</p> <hr/> <h2 id="what-tarjim-does">What Tarjim Does</h2> <p>Tarjim takes a scanned Arabic PDF, runs OCR to extract the text line by line, translates each line offline, and writes the translated text back onto the same PDF — replacing the original Arabic in-place.</p> <p>The result is a new PDF that looks like the original layout, but in English (or Indonesian, or other languages).</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:  Scanned Arabic PDF  (just images of Arabic text)
            ↓
Output: Translated PDF  (English/Indonesian text in the same positions)
</code></pre></div></div> <p>The entire pipeline runs <strong>offline</strong> after the initial model download. No API keys. No internet. No data leaves your machine. This was a hard requirement for me — these are religious texts, and privacy matters.</p> <hr/> <h2 id="how-it-works">How It Works</h2> <p>The pipeline has five steps:</p> <pre><code class="language-mermaid">graph TD
    A[Scanned Arabic PDF] --&gt; B[Render pages to images&lt;br/&gt;PyMuPDF @ 300 DPI]
    B --&gt; C[Arabic OCR&lt;br/&gt;Surya OCR → text lines + bounding boxes]
    C --&gt; D{Direct package available?}
    D --&gt;|ar → en| E[Direct translation&lt;br/&gt;Argos Translate]
    D --&gt;|ar → id / others| F[Pivot translation&lt;br/&gt;ar → en → id]
    E --&gt; G[Overlay translated text&lt;br/&gt;PIL with white box + word-wrap]
    F --&gt; G
    G --&gt; H[Save as new PDF]
</code></pre> <h3 id="step-1--render-pdf-to-images">Step 1 — Render PDF to Images</h3> <p>Since the input is a scanned document (images, not text), we first render each page into a high-resolution PIL image using PyMuPDF at 300 DPI. Higher DPI means better OCR accuracy.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">zoom</span> <span class="o">=</span> <span class="mi">300</span> <span class="o">/</span> <span class="mf">72.0</span>  <span class="c1"># PDF baseline is 72 DPI
</span><span class="n">matrix</span> <span class="o">=</span> <span class="n">pymupdf</span><span class="p">.</span><span class="nc">Matrix</span><span class="p">(</span><span class="n">zoom</span><span class="p">,</span> <span class="n">zoom</span><span class="p">)</span>
<span class="n">pix</span> <span class="o">=</span> <span class="n">page</span><span class="p">.</span><span class="nf">get_pixmap</span><span class="p">(</span><span class="n">matrix</span><span class="o">=</span><span class="n">matrix</span><span class="p">)</span>
<span class="n">image</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="nf">frombytes</span><span class="p">(</span><span class="sh">"</span><span class="s">RGB</span><span class="sh">"</span><span class="p">,</span> <span class="p">(</span><span class="n">pix</span><span class="p">.</span><span class="n">width</span><span class="p">,</span> <span class="n">pix</span><span class="p">.</span><span class="n">height</span><span class="p">),</span> <span class="n">pix</span><span class="p">.</span><span class="n">samples</span><span class="p">)</span>
</code></pre></div></div> <h3 id="step-2--ocr-with-surya">Step 2 — OCR with Surya</h3> <p>This is the most critical step. I tried three OCR engines before settling on Surya:</p> <ul> <li><strong>Tesseract</strong> — The classic. Arabic support exists, but accuracy on old scanned texts is poor. Struggles with naskh and ruq’ah scripts.</li> <li><strong>PaddleOCR</strong> — Better accuracy, but heavier setup and the Arabic model needed extra configuration.</li> <li><strong>Surya OCR</strong> — Built specifically for document-level OCR, supports Arabic out of the box, and returns text lines with precise bounding boxes. This is what I use.</li> </ul> <p>Surya returns a structured prediction: for each page, you get a list of <code class="language-plaintext highlighter-rouge">text_lines</code>, each with <code class="language-plaintext highlighter-rouge">.text</code> (the recognized Arabic string) and <code class="language-plaintext highlighter-rouge">.bbox</code> (the pixel coordinates <code class="language-plaintext highlighter-rouge">x1, y1, x2, y2</code>).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">surya.recognition</span> <span class="kn">import</span> <span class="n">RecognitionPredictor</span>
<span class="kn">from</span> <span class="n">surya.detection</span> <span class="kn">import</span> <span class="n">DetectionPredictor</span>

<span class="n">recognition_predictor</span> <span class="o">=</span> <span class="nc">RecognitionPredictor</span><span class="p">(</span><span class="n">foundation_predictor</span><span class="p">)</span>
<span class="n">detection_predictor</span> <span class="o">=</span> <span class="nc">DetectionPredictor</span><span class="p">()</span>

<span class="n">predictions</span> <span class="o">=</span> <span class="nf">recognition_predictor</span><span class="p">([</span><span class="n">image</span><span class="p">],</span> <span class="n">det_predictor</span><span class="o">=</span><span class="n">detection_predictor</span><span class="p">)</span>
<span class="n">page</span> <span class="o">=</span> <span class="n">predictions</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>

<span class="k">for</span> <span class="n">line</span> <span class="ow">in</span> <span class="n">page</span><span class="p">.</span><span class="n">text_lines</span><span class="p">:</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">text</span><span class="p">)</span>   <span class="c1"># "بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيمِ"
</span>    <span class="nf">print</span><span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">bbox</span><span class="p">)</span>   <span class="c1"># (42, 120, 890, 158)
</span></code></pre></div></div> <h3 id="step-3--translate-with-argos-translate">Step 3 — Translate with Argos Translate</h3> <p>Argos Translate is a fully offline, open-source machine translation library backed by OpenNMT models. Once packages are downloaded, no network is needed.</p> <p>The interesting engineering problem here was supporting Indonesian (Bahasa Indonesia) — there is no direct Arabic → Indonesian package in Argos. The solution: <strong>pivot translation</strong>.</p> <p>I’ll explain this in detail in the next section.</p> <h3 id="step-4--overlay-translated-text">Step 4 — Overlay Translated Text</h3> <p>For each detected text line, I:</p> <ol> <li>Draw a <strong>white rectangle</strong> over the original Arabic text (covering it completely)</li> <li>Calculate an appropriate font size based on the bounding box height</li> <li>Draw the translated text with <strong>word-wrapping</strong> to fit inside the same bounding box</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Cover the original Arabic
</span><span class="n">draw</span><span class="p">.</span><span class="nf">rectangle</span><span class="p">([</span><span class="n">x1</span><span class="p">,</span> <span class="n">y1</span><span class="p">,</span> <span class="n">x2</span><span class="p">,</span> <span class="n">y2</span><span class="p">],</span> <span class="n">fill</span><span class="o">=</span><span class="sh">"</span><span class="s">white</span><span class="sh">"</span><span class="p">,</span> <span class="n">outline</span><span class="o">=</span><span class="sh">"</span><span class="s">white</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Calculate font size from bbox height
</span><span class="n">font_size</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="nf">int</span><span class="p">((</span><span class="n">y2</span> <span class="o">-</span> <span class="n">y1</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.8</span><span class="p">))</span>
<span class="n">font</span> <span class="o">=</span> <span class="n">ImageFont</span><span class="p">.</span><span class="nf">truetype</span><span class="p">(</span><span class="n">font_path</span><span class="p">,</span> <span class="n">font_size</span><span class="p">)</span>

<span class="c1"># Word-wrap and draw translated text
</span><span class="nf">draw_text_in_box</span><span class="p">(</span><span class="n">draw</span><span class="p">,</span> <span class="n">translated_text</span><span class="p">,</span> <span class="n">bbox</span><span class="p">,</span> <span class="n">font</span><span class="p">)</span>
</code></pre></div></div> <p>There’s also a <code class="language-plaintext highlighter-rouge">clean</code> mode that starts from a blank white page — useful if you just want the translated text without the original scanned image behind it.</p> <h3 id="step-5--save-as-pdf">Step 5 — Save as PDF</h3> <p>All modified page images are saved back into a multi-page PDF using Pillow.</p> <hr/> <h2 id="the-engineering-decision-im-most-proud-of-smart-translation-routing">The Engineering Decision I’m Most Proud Of: Smart Translation Routing</h2> <p>This was the most interesting design challenge.</p> <p>Argos Translate has language packages that you install individually — <code class="language-plaintext highlighter-rouge">ar→en</code>, <code class="language-plaintext highlighter-rouge">en→id</code>, <code class="language-plaintext highlighter-rouge">en→fr</code>, etc. There is an <code class="language-plaintext highlighter-rouge">ar→en</code> package (Arabic to English). But there is <strong>no</strong> <code class="language-plaintext highlighter-rouge">ar→id</code> package (Arabic to Indonesian).</p> <p>So how do you translate Arabic to Indonesian? You chain two translations:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Arabic text → [ar→en model] → English text → [en→id model] → Indonesian text
</code></pre></div></div> <p>This is called <strong>pivot translation through English</strong>.</p> <p>The challenge is making this seamless. A user just wants to say <code class="language-plaintext highlighter-rouge">--lang id</code> and get Indonesian output. They shouldn’t need to know about pivoting.</p> <p>Here’s how I implemented it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">setup_argos_translation</span><span class="p">(</span><span class="n">from_code</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">to_code</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Returns </span><span class="sh">'</span><span class="s">direct</span><span class="sh">'</span><span class="s"> or </span><span class="sh">'</span><span class="s">pivot:en</span><span class="sh">'</span><span class="s"> depending on available packages.
    Auto-installs whatever is needed.
    </span><span class="sh">"""</span>
    <span class="c1"># 1. Try to find a direct package (e.g., ar→id)
</span>    <span class="n">direct_package</span> <span class="o">=</span> <span class="nf">find_package</span><span class="p">(</span><span class="n">from_code</span><span class="p">,</span> <span class="n">to_code</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">direct_package</span><span class="p">:</span>
        <span class="nf">install</span><span class="p">(</span><span class="n">direct_package</span><span class="p">)</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">direct</span><span class="sh">"</span>

    <span class="c1"># 2. No direct package — install pivot packages (ar→en + en→id)
</span>    <span class="nf">install</span><span class="p">(</span><span class="nf">find_package</span><span class="p">(</span><span class="n">from_code</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">))</span>
    <span class="nf">install</span><span class="p">(</span><span class="nf">find_package</span><span class="p">(</span><span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">,</span> <span class="n">to_code</span><span class="p">))</span>
    <span class="k">return</span> <span class="sh">"</span><span class="s">pivot:en</span><span class="sh">"</span>
</code></pre></div></div> <p>And in <code class="language-plaintext highlighter-rouge">translate_text()</code>, if the pair is known to be a pivot pair, it chains two calls:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">translate_text</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">from_code</span><span class="o">=</span><span class="sh">"</span><span class="s">ar</span><span class="sh">"</span><span class="p">,</span> <span class="n">to_code</span><span class="o">=</span><span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">):</span>
    <span class="k">if</span> <span class="sh">"</span><span class="s">ar-&gt;id</span><span class="sh">"</span> <span class="ow">in</span> <span class="n">_PIVOT_PAIRS</span><span class="p">:</span>
        <span class="c1"># Two-step: ar → en → id
</span>        <span class="n">en_text</span> <span class="o">=</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="sh">"</span><span class="s">ar</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">en_text</span><span class="p">,</span> <span class="sh">"</span><span class="s">en</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># Direct: ar → en
</span>    <span class="k">return</span> <span class="n">argos</span><span class="p">.</span><span class="nf">translate</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">from_code</span><span class="p">,</span> <span class="n">to_code</span><span class="p">)</span>
</code></pre></div></div> <p>The system remembers which pairs need pivoting at runtime. It even <strong>auto-discovers</strong> the need: if a direct translation call fails, it automatically retries via the English pivot and marks that pair for future calls.</p> <p>The route selection is fully transparent through the CLI:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ python -m src.cli -i kitab.pdf -o kitab_id.pdf --lang id --verbose

Translation route: ar → en → id (pivot through English, no direct package available)
</code></pre></div></div> <h3 id="translation-routing-table">Translation Routing Table</h3> <table> <thead> <tr> <th>Target Language</th> <th>Route</th> <th>Packages Needed</th> </tr> </thead> <tbody> <tr> <td>English (<code class="language-plaintext highlighter-rouge">en</code>)</td> <td>Direct</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code></td> </tr> <tr> <td>Indonesian (<code class="language-plaintext highlighter-rouge">id</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→id</code></td> </tr> <tr> <td>Malay (<code class="language-plaintext highlighter-rouge">ms</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→ms</code></td> </tr> <tr> <td>French (<code class="language-plaintext highlighter-rouge">fr</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→fr</code></td> </tr> <tr> <td>German (<code class="language-plaintext highlighter-rouge">de</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→de</code></td> </tr> <tr> <td>Spanish (<code class="language-plaintext highlighter-rouge">es</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→es</code></td> </tr> <tr> <td>Turkish (<code class="language-plaintext highlighter-rouge">tr</code>)</td> <td>Pivot</td> <td><code class="language-plaintext highlighter-rouge">ar→en</code> + <code class="language-plaintext highlighter-rouge">en→tr</code></td> </tr> </tbody> </table> <hr/> <h2 id="getting-started">Getting Started</h2> <h3 id="installation">Installation</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/scrowten/tarjim.git
<span class="nb">cd </span>tarjim

python <span class="nt">-m</span> venv venv
<span class="nb">source </span>venv/bin/activate  <span class="c"># Windows: venv\Scripts\activate</span>

pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
</code></pre></div></div> <p>On first run, Surya OCR models (~2–3 GB) and Argos translation packages download automatically. After that: fully offline.</p> <h3 id="translate-arabic--english">Translate Arabic → English</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> src.cli <span class="nt">--input</span> kitab.pdf <span class="nt">--output</span> kitab_en.pdf <span class="nt">--lang</span> en
</code></pre></div></div> <h3 id="translate-arabic--indonesian">Translate Arabic → Indonesian</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> src.cli <span class="nt">--input</span> kitab.pdf <span class="nt">--output</span> kitab_id.pdf <span class="nt">--lang</span> <span class="nb">id</span>
</code></pre></div></div> <p>The system automatically uses the ar→en→id pivot route. You don’t need to do anything special.</p> <h3 id="web-ui">Web UI</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>uvicorn src.api:app <span class="nt">--port</span> 8000
<span class="c"># Open http://localhost:8000</span>
</code></pre></div></div> <p>Upload a PDF, pick a language, click Translate. The UI shows the translation route so you know what’s happening under the hood.</p> <h3 id="docker">Docker</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker build <span class="nt">-t</span> tarjim <span class="nb">.</span>
docker run <span class="nt">-p</span> 8000:8000 tarjim
</code></pre></div></div> <p>The Docker image pre-downloads both the <code class="language-plaintext highlighter-rouge">ar→en</code> and <code class="language-plaintext highlighter-rouge">en→id</code> packages at build time.</p> <h3 id="python-api">Python API</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">src.core.pdf_handler</span> <span class="kn">import</span> <span class="n">process_pdf</span>

<span class="c1"># Arabic → Indonesian
</span><span class="nf">process_pdf</span><span class="p">(</span>
    <span class="n">input_path</span><span class="o">=</span><span class="sh">"</span><span class="s">al-qawaid-al-arba.pdf</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">output_path</span><span class="o">=</span><span class="sh">"</span><span class="s">al-qawaid-al-arba_id.pdf</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">target_lang</span><span class="o">=</span><span class="sh">"</span><span class="s">id</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">overlay_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">replace</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># or "clean" for white background
</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="project-structure">Project Structure</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tarjim/
├── src/
│   ├── cli.py                    # Command-line interface
│   ├── api.py                    # FastAPI web server
│   └── core/
│       ├── pdf_handler.py        # Pipeline orchestrator
│       ├── ocr_surya.py          # Surya OCR wrapper
│       ├── translator_argos.py   # Argos Translate + pivot routing
│       └── utils.py              # Text overlay, fonts, word-wrap
├── static/
│   ├── index.html                # Web UI
│   └── styles.css
├── tests/                        # Unit tests
├── Dockerfile
└── requirements.txt
</code></pre></div></div> <p>The core modules are intentionally small and single-purpose. <code class="language-plaintext highlighter-rouge">translator_argos.py</code> knows nothing about PDFs. <code class="language-plaintext highlighter-rouge">ocr_surya.py</code> knows nothing about translation. <code class="language-plaintext highlighter-rouge">pdf_handler.py</code> orchestrates everything.</p> <hr/> <h2 id="challenges--lessons-learned">Challenges &amp; Lessons Learned</h2> <p><strong>OCR accuracy on old texts is hard.</strong> Surya is the best open-source option available, but classical Arabic manuscript fonts are different from modern typeset text. Vowel diacritics (<em>tashkeel</em>) especially trip up OCR models. For perfectly clean modern typeset kitab, results are good. For old handwritten manuscripts, there’s room to improve.</p> <p><strong>Translation quality through pivoting is lossy.</strong> Chaining <code class="language-plaintext highlighter-rouge">ar→en→id</code> means any error in the first step gets propagated and amplified in the second. The <code class="language-plaintext highlighter-rouge">ar→en</code> Argos model performs reasonably well on straightforward prose, but classical Arabic religious vocabulary — <em>tawhid</em>, <em>shirk</em>, <em>sunnah</em>, <em>bid’ah</em> — often gets transliterated rather than translated, which is actually appropriate for Islamic texts.</p> <p><strong>Font fitting is a solved problem — just tedious.</strong> Getting English text to fit into the same bounding box as Arabic text is fiddly. Arabic is compact horizontally; English words for the same concept are often longer. I handle this with dynamic font sizing and word-wrapping, but very dense Arabic pages still get tight.</p> <p><strong>Lazy imports matter for usability.</strong> Early versions of the code eagerly imported PyMuPDF and Surya at the top of every file. This meant that even <code class="language-plaintext highlighter-rouge">import src.core.utils</code> would try to load multi-gigabyte models. Moving to lazy imports (<code class="language-plaintext highlighter-rouge">__getattr__</code> on the package <code class="language-plaintext highlighter-rouge">__init__.py</code>) made the module usable in testing environments without all dependencies present.</p> <hr/> <h2 id="whats-next">What’s Next</h2> <p>The project is functional but there’s a clear path forward:</p> <ul> <li><strong>Better handling of dense pages</strong> — Auto-shrink font, or flow overflow text to margins</li> <li><strong>Bilingual output mode</strong> — Show original Arabic alongside the translation (side-by-side columns or interleaved pages)</li> <li><strong>Batch processing</strong> — Translate an entire folder of kitab PDFs in one command</li> <li><strong>Confidence scoring</strong> — Flag lines where OCR or translation confidence is low</li> <li><strong>Better support for tashkeel</strong> — Strip or handle vowel diacritics before OCR to improve accuracy</li> <li><strong>Table of contents preservation</strong> — Detect and translate structural elements, not just body text</li> <li><strong>Optional cloud API mode</strong> — Hook into a higher-quality translation API (OpenAI, Google) for users who prefer accuracy over privacy</li> </ul> <hr/> <h2 id="try-it">Try It</h2> <p>The project is open source under MIT license.</p> <p><strong>GitHub:</strong> <a href="https://github.com/scrowten/tarjim">github.com/scrowten/tarjim</a></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/scrowten/tarjim.git
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
python <span class="nt">-m</span> src.cli <span class="nt">--input</span> your_kitab.pdf <span class="nt">--output</span> translated.pdf <span class="nt">--lang</span> en
</code></pre></div></div> <p>If you work with Arabic documents — religious texts, academic papers, historical archives — I hope this tool is useful to you. Contributions, issues, and suggestions are very welcome.</p> <hr/> <p><em>Built with <a href="https://github.com/VikParuchuri/surya">Surya OCR</a>, <a href="https://github.com/argosopentech/argos-translate">Argos Translate</a>, <a href="https://github.com/pymupdf/PyMuPDF">PyMuPDF</a>, and <a href="https://fastapi.tiangolo.com/">FastAPI</a>.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="nlp"/><category term="open-source"/><category term="ocr"/><category term="translation"/><category term="arabic"/><category term="nlp"/><category term="python"/><summary type="html"><![CDATA[How I built Tarjim, an open-source offline OCR + translation pipeline for scanned Arabic kitab PDFs, using Surya OCR and Argos Translate with smart pivot routing for Indonesian.]]></summary></entry><entry><title type="html">🚀 Build Your First AI Agent with Google’s ADK (Part 1)</title><link href="https://scrowten.github.io/blog/2025/google-adk-part-1/" rel="alternate" type="text/html" title="🚀 Build Your First AI Agent with Google’s ADK (Part 1)"/><published>2025-11-24T14:25:00+00:00</published><updated>2025-11-24T14:25:00+00:00</updated><id>https://scrowten.github.io/blog/2025/google-adk-part-1</id><content type="html" xml:base="https://scrowten.github.io/blog/2025/google-adk-part-1/"><![CDATA[<h3 id="from-prompt--thought--action--result">From Prompt → Thought → Action → Result</h3> <p>AI is evolving fast—and it’s no longer limited to the classic <em>“you ask, it answers”</em> pattern.<br/> Today’s AI can do something far more powerful: <strong>act like an intelligent problem-solver</strong>.</p> <p>Modern AI <strong>agents</strong> don’t just generate text.</p> <p>They can <strong>think through a problem</strong>, <strong>decide what tools to use</strong>, <strong>take meaningful actions</strong>, and <strong>learn from the results</strong> before giving you a final answer.</p> <p>It’s like upgrading from a calculator… to a smart assistant that can research, reason, and execute tasks on your behalf.</p> <p>In this guide, you’ll get hands-on and build your <strong>first real AI agent</strong> using Google’s <strong>Agent Development Kit (ADK)</strong>—the same framework featured in the official Kaggle 5-Day Agents Course.</p> <p>No prior experience required. Just curiosity, a few lines of code, and you’re ready to create an AI that doesn’t just respond…<br/> <strong>it gets things done.</strong> 🚀</p> <p>By the end, you will:</p> <ul> <li>✔️ Install and configure ADK</li> <li>✔️ Set up Gemini API authentication</li> <li>✔️ Build your first agent with reasoning + tools</li> <li>✔️ Watch your agent perform an action (Google Search!)</li> <li>✔️ Explore the ADK Web Interface</li> </ul> <p>Let’s begin. 🚀</p> <hr/> <h1 id="️-1-setup">⚙️ 1. Setup</h1> <h2 id="11-install-dependencies">1.1 Install Dependencies</h2> <p>If you’re using Kaggle Notebooks, <strong>google-adk</strong> is already installed. To install it locally:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>google-adk
</code></pre></div></div> <hr/> <h2 id="12-configure-your-gemini-api-key">1.2 Configure Your Gemini API Key</h2> <p>ADK uses <strong>Gemini models</strong>, which require authentication.</p> <h3 id="1-create-an-api-key">1) Create an API Key</h3> <p>Go to <strong>Google AI Studio</strong> → Generate an API key.</p> <h3 id="2-add-your-key-to-your-environment">2) Add your key to your environment</h3> <p>Inside your project, create a <code class="language-plaintext highlighter-rouge">.env</code> file:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">GOOGLE_API_KEY</span><span class="o">=</span><span class="s2">"YOUR_KEY_HERE"</span>
</code></pre></div></div> <hr/> <h2 id="13-import-adk-components">1.3 Import ADK Components</h2> <p>These components give your agent LLM reasoning, tools, and execution runtime.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">google.adk.agents</span> <span class="kn">import</span> <span class="n">Agent</span>
<span class="kn">from</span> <span class="n">google.adk.models.google_llm</span> <span class="kn">import</span> <span class="n">Gemini</span>
<span class="kn">from</span> <span class="n">google.adk.runners</span> <span class="kn">import</span> <span class="n">InMemoryRunner</span>
<span class="kn">from</span> <span class="n">google.adk.tools</span> <span class="kn">import</span> <span class="n">google_search</span>
<span class="kn">from</span> <span class="n">google.genai</span> <span class="kn">import</span> <span class="n">types</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ ADK components imported successfully.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="14-configure-retry-behavior">1.4 Configure Retry Behavior</h2> <p>LLMs sometimes hit rate limits or temporary service issues. Retries help make your agent more reliable.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">retry_config</span> <span class="o">=</span> <span class="n">types</span><span class="p">.</span><span class="nc">HttpRetryOptions</span><span class="p">(</span>
    <span class="n">attempts</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>            <span class="c1"># Max retries
</span>    <span class="n">exp_base</span><span class="o">=</span><span class="mi">7</span><span class="p">,</span>            <span class="c1"># Exponential backoff multiplier
</span>    <span class="n">initial_delay</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>       <span class="c1"># Delay before first retry
</span>    <span class="n">http_status_codes</span><span class="o">=</span><span class="p">[</span><span class="mi">429</span><span class="p">,</span> <span class="mi">500</span><span class="p">,</span> <span class="mi">503</span><span class="p">,</span> <span class="mi">504</span><span class="p">]</span>  <span class="c1"># Retryable HTTP error codes
</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h1 id="-2-build-your-first-ai-agent">🤖 2. Build Your First AI Agent</h1> <h2 id="21-what-is-an-ai-agent">2.1 What <em>Is</em> an AI Agent?</h2> <p>A normal LLM:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prompt → LLM → Text
</code></pre></div></div> <p>An AI agent:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prompt → Agent → Thought → Action → Observation → Final Answer
</code></pre></div></div> <p>Your agent will be able to:</p> <ul> <li>Interpret a question</li> <li>Decide whether to search the web</li> <li>Use Google Search</li> <li>Combine results into a final answer</li> </ul> <hr/> <h2 id="22-define-your-agent">2.2 Define Your Agent</h2> <p>Here we describe:</p> <ul> <li><strong>Name</strong> &amp; <strong>description</strong></li> <li><strong>Model</strong> (Gemini 2.5 Flash Lite)</li> <li><strong>Instructions</strong> (how the agent should behave)</li> <li><strong>Tools</strong> (the actions it can take — we give it Google Search)</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">root_agent</span> <span class="o">=</span> <span class="nc">Agent</span><span class="p">(</span>
    <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">helpful_assistant</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">model</span><span class="o">=</span><span class="nc">Gemini</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gemini-2.5-flash-lite</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">retry_options</span><span class="o">=</span><span class="n">retry_config</span>
    <span class="p">),</span>
    <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">A simple agent that can answer general questions.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">instruction</span><span class="o">=</span><span class="sh">"</span><span class="s">You are a helpful assistant. Use Google Search for current info or if unsure.</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">tools</span><span class="o">=</span><span class="p">[</span><span class="n">google_search</span><span class="p">],</span>
<span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ Root Agent defined.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="23-run-your-agent">2.3 Run Your Agent</h2> <p>To run an agent, you need a <strong>Runner</strong>. This acts as the orchestrator that:</p> <ul> <li>Keeps track of the conversation</li> <li>Sends messages to the agent</li> <li>Handles tool calls &amp; results</li> </ul> <h3 id="a-create-the-runner">a) Create the Runner</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">runner</span> <span class="o">=</span> <span class="nc">InMemoryRunner</span><span class="p">(</span><span class="n">agent</span><span class="o">=</span><span class="n">root_agent</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">✅ Runner created.</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="b-ask-the-agent-a-question">b) Ask the Agent a Question</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">runner</span><span class="p">.</span><span class="nf">run_debug</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">What is Agent Development Kit from Google? What languages is the SDK available in?</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div> <p>This method:</p> <ul> <li>Creates a temporary session</li> <li>Allows the agent to think</li> <li>Lets it call Google Search</li> <li>Returns the final answer</li> </ul> <p>Perfect for prototyping!</p> <hr/> <h2 id="24-what-just-happened">2.4 What Just Happened?</h2> <p>Your agent:</p> <ol> <li>Read your question</li> <li>Realized it needed <strong>current information</strong></li> <li>Called the <strong>Google Search tool</strong></li> <li>Reviewed results</li> <li>Generated the final answer</li> </ol> <p>This is the foundation of AI agents: <strong>LLM → reasoning → tool choice → action → observation → improved answer</strong></p> <p>To see a full trace (thoughts, actions, observations), use the ADK Web UI next.</p> <hr/> <h1 id="-3-explore-the-adk-web-interface">💻 3. Explore the ADK Web Interface</h1> <p>ADK includes a browser-based UI for:</p> <ul> <li>Debugging agents</li> <li>Viewing traces</li> <li>Inspecting tool calls</li> <li>Exploring sessions</li> </ul> <h2 id="31-create-a-sample-agent">3.1 Create a Sample Agent</h2> <p>Run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adk create sample-agent <span class="nt">--model</span> gemini-2.5-flash-lite <span class="nt">--api_key</span> <span class="nv">$GOOGLE_API_KEY</span>
</code></pre></div></div> <p>This generates:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sample-agent/
├── agent.py
├── .env
└── __init__.py
</code></pre></div></div> <h2 id="32-get-your-proxy-url-kaggle-only">3.2 Get Your Proxy URL (Kaggle only)</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">url_prefix</span> <span class="o">=</span> <span class="nf">get_adk_proxy_url</span><span class="p">()</span>
</code></pre></div></div> <h2 id="33-launch-the-web-ui">3.3 Launch the Web UI</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>adk web <span class="nt">--url_prefix</span> <span class="o">{</span>url_prefix<span class="o">}</span>
</code></pre></div></div> <p>📌 <strong>Important:</strong> <strong>Do NOT share your proxy link</strong> — it contains an authentication token.</p> <hr/> <h1 id="-congratulations">🎉 Congratulations!</h1> <p>You just created your <strong>first AI agent</strong> using Google’s ADK!</p> <p>Key takeaways:</p> <ul> <li>Agents ≠ plain LLMs</li> <li>Agents <strong>reason</strong>, <strong>use tools</strong>, and <strong>act</strong></li> <li>ADK makes building agents simple and structured</li> <li>You now have a working agent that can search the internet</li> </ul> <p>Next steps:</p> <ul> <li>Try adding more tools</li> <li>Build multi-agent systems</li> <li>Explore advanced session management</li> <li>Deploy agents in real applications</li> </ul> <hr/>]]></content><author><name></name></author><category term="machine-learning"/><category term="llm"/><category term="agents"/><category term="adk"/><category term="agents"/><category term="llm"/><summary type="html"><![CDATA[Concise agentic tutorial using Google's ADK]]></summary></entry><entry><title type="html">Demystifying Visual Transformer (ViT)</title><link href="https://scrowten.github.io/blog/2021/vit-paper/" rel="alternate" type="text/html" title="Demystifying Visual Transformer (ViT)"/><published>2021-08-19T21:01:00+00:00</published><updated>2021-08-19T21:01:00+00:00</updated><id>https://scrowten.github.io/blog/2021/vit-paper</id><content type="html" xml:base="https://scrowten.github.io/blog/2021/vit-paper/"><![CDATA[<h1 id="-paper-summary-an-image-is-worth-16x16-words--transformers-for-image-recognition-at-scale">🧠 Paper Summary: <em>An Image is Worth 16x16 Words — Transformers for Image Recognition at Scale</em></h1> <p><strong>Authors:</strong> Alexey Dosovitskiy et al.<br/> <strong>Published by:</strong> Google Research, Brain Team (2020)<br/> <strong>Link:</strong> <a href="https://arxiv.org/pdf/2010.11929">arxiv.org/pdf/2010.11929</a></p> <hr/> <h2 id="-overview">📌 Overview</h2> <p>This paper introduces the <strong>Vision Transformer (ViT)</strong> — the first major attempt to apply a <strong>Transformer architecture</strong>, originally designed for NLP, directly to image classification tasks.<br/> The key idea: instead of processing pixels through convolutional layers, <strong>an image is split into patches</strong>, each treated as a “token” (similar to words in a sentence).</p> <p>ViT demonstrates that with <strong>sufficient data and compute</strong>, Transformers can outperform CNNs in image recognition — marking a major shift in computer vision research.</p> <hr/> <h2 id="-key-concepts">🧩 Key Concepts</h2> <h3 id="1-patch-embedding">1. <strong>Patch Embedding</strong></h3> <ul> <li>The input image (e.g., 224×224) is divided into fixed-size <strong>patches</strong> (e.g., 16×16 pixels).</li> <li>Each patch is <strong>flattened</strong> and linearly projected to form a <strong>vector embedding</strong>.</li> <li>These patch embeddings serve as input tokens for the Transformer encoder.</li> </ul> <h3 id="2-positional-encoding">2. <strong>Positional Encoding</strong></h3> <ul> <li>Since Transformers have no inherent notion of spatial order, <strong>positional embeddings</strong> are added to patch embeddings to preserve spatial relationships.</li> </ul> <h3 id="3-transformer-encoder">3. <strong>Transformer Encoder</strong></h3> <ul> <li>The encoder consists of <strong>multi-head self-attention (MHSA)</strong> and <strong>MLP blocks</strong>, similar to the standard NLP Transformer (Vaswani et al., 2017).</li> <li>Layer normalization and residual connections are applied after each sub-layer.</li> </ul> <h3 id="4-classification-token-cls">4. <strong>Classification Token ([CLS])</strong></h3> <ul> <li>A special learnable token is prepended to the sequence of patch embeddings.</li> <li>The final representation of this token is used for <strong>image classification</strong>.</li> </ul> <hr/> <h2 id="️-training-setup">⚙️ Training Setup</h2> <ul> <li><strong>Pretraining Dataset:</strong> JFT-300M (≈300M images, 18k classes) and ImageNet-21k (14M images, 21k classes).</li> <li><strong>Fine-tuning:</strong> After pretraining, the model is fine-tuned on smaller datasets like ImageNet, CIFAR-100, and VTAB.</li> <li><strong>Optimization:</strong> Adam optimizer with a cosine learning rate schedule and heavy data augmentation.</li> </ul> <hr/> <h2 id="-results">📈 Results</h2> <table> <thead> <tr> <th>Model</th> <th>Pretraining Dataset</th> <th>ImageNet Accuracy</th> <th>Params</th> </tr> </thead> <tbody> <tr> <td>ViT-B/16</td> <td>ImageNet-21k</td> <td>84.0%</td> <td>86M</td> </tr> <tr> <td>ViT-L/16</td> <td>JFT-300M</td> <td>88.5%</td> <td>307M</td> </tr> <tr> <td>ViT-H/14</td> <td>JFT-300M</td> <td>90.0%</td> <td>632M</td> </tr> </tbody> </table> <ul> <li>ViT outperforms <strong>ResNet</strong> and other CNN-based models when pretrained on large datasets.</li> <li>On smaller datasets (without large-scale pretraining), ViT underperforms due to weaker inductive biases compared to CNNs.</li> </ul> <hr/> <h2 id="-insights--contributions">🧠 Insights &amp; Contributions</h2> <ol> <li><strong>Transformers can scale to vision tasks</strong> with sufficient data and computational power.</li> <li><strong>CNN inductive biases</strong> (translation invariance, locality) are not strictly necessary for high performance — they can be learned from data.</li> <li>ViT models <strong>transfer well</strong> across different datasets via fine-tuning.</li> <li>Demonstrated the <strong>emerging scalability law</strong> — model performance increases predictably with data and compute.</li> </ol> <hr/> <h2 id="️-pros--cons">⚖️ Pros &amp; Cons</h2> <table> <thead> <tr> <th>Pros</th> <th>Cons</th> </tr> </thead> <tbody> <tr> <td>Simple, elegant architecture — no convolutions.</td> <td>Requires massive data for effective training.</td> </tr> <tr> <td>Strong transfer learning performance.</td> <td>Computationally expensive at large scale.</td> </tr> <tr> <td>Flexible — easily extended to detection, segmentation, etc.</td> <td>Poor inductive bias for small data settings.</td> </tr> </tbody> </table> <hr/> <h2 id="-follow-up-work">🔍 Follow-up Work</h2> <ul> <li><strong>DeiT (Data-efficient Image Transformer)</strong>: Improves ViT training efficiency with smaller datasets using knowledge distillation.</li> <li><strong>Swin Transformer</strong>: Introduces hierarchical structure and local attention windows for dense prediction tasks.</li> <li><strong>Hybrid Models</strong>: Combine CNN feature extractors with ViT-style attention blocks.</li> </ul> <hr/> <h2 id="-takeaway">🧭 Takeaway</h2> <blockquote> <p><strong>“ViT marks the turning point where pure Transformers became competitive with CNNs in computer vision — given enough data.”</strong></p> </blockquote> <p>It simplifies the architecture but relies on <strong>data scale</strong> and <strong>transfer learning</strong>, setting the foundation for the transformer revolution in visual tasks.</p> <hr/> <p><em>Prepared for technical sharing session — concise summary by Rizky.</em></p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformer"/><category term="machine-learning"/><category term="image-processing"/><category term="vit"/><summary type="html"><![CDATA[Paper Summary of Visual Transformer (ViT)]]></summary></entry></feed>